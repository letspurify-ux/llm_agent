# Backend 검토 기록

2026-09-06. 요청 범위는 backend 전체 코드 검토, 결함 재현 후 수정, 수정 후 정상 동작 검증,
production 결함의 정식 회귀 테스트 추가다. 시작 시 backend 변경 사항은 없었고 기존 frontend·README 변경은 보존했다.

`backend/src` 16개 모듈, SQL 5개 파일, 환경 설정·패키지·실행 스크립트를 읽고 현재 계약인
`context.md`와 대조했다. 기존 테스트는 417개가 모두 통과했지만 아래 새 재현 테스트에서 결함이 확인됐다.

같은 날 후속 검토에서는 기존 수정 상태에서 442개 전체 테스트를 다시 통과시킨 뒤 전체 소스와
실행·SQL 파일을 재검토했다. 숫자 정밀도와 문서 해시 경계에서 추가 결함 2개를 실제 DB로 재현했다.
공유 숫자 변환 모듈 `numbers.js`를 포함한 최종 검토 대상은 `backend/src` 17개 모듈이다.

| 결함 | 수정 전 재현 결과 | 수정 | 정식 테스트 |
|---|---|---|---|
| LLM 완료 신호 처리 | SSE `[DONE]` 뒤 연결이 열려 있으면 정상 답을 버리고 재시도 후 null 반환 | 완료 신호에서 읽기를 종료하고 스트림 취소 | `llm-openai.test.js` |
| SSE 이벤트 조립 | 여러 `data:` 줄로 된 정상 이벤트에서 답을 찾지 못함 | 이벤트 단위 조립, LF·CR·CRLF 및 바이트 경계 처리 | `llm-openai.test.js` |
| 임베딩 응답 검증 | 잘못된 구조를 일시 장애로 분류하고 숫자가 아닌 벡터를 허용 | 응답 구조·index·유한 숫자 검증 | `embedding.test.js` |
| 검색 근거 누락 | 같은 문서의 1·3번 청크가 적중해도 보충 조회 실패 시 1번만 반환 | 확보한 적중을 보존하고 누락 구간을 독립 항목으로 반환 | `search.test.js` |
| 동기화 해시 경쟁 | 해시 조회와 본문 조회 사이 변경·원복 시 다른 본문의 청크와 벡터를 계속 최신으로 판단 | 실제 본문과 해시를 같은 SELECT에서 읽어 저장 | `embed-sync.test.js` |
| 확대 중 근거 교체 | 검색 후 원문 변경 시 expand가 기존 근거를 교체 | 확보한 청크와 달라진 판본으로 확대하지 않고 기존 근거 유지 | `agent.test.js` |
| Oracle 풀 교체 | 접속 정보 변경 시 기존 풀의 획득 대기·조회 요청을 중간에 종료 | 획득 대기부터 반납까지 사용자 수를 추적하고 마지막 반납 후 이전 풀 정리 | `oracle-pool.test.js` |
| Oracle 초기화 타임아웃 | ALTER SESSION 실행 시 callTimeout이 0 | 세션 초기화부터 조회 타임아웃 적용 | `oracle-pool.test.js` |
| 서버 PID 소유 확인 | 다른 프로젝트의 src/server.js를 이미 실행 중인 서버로 판단하거나 종료 | Node 실행 파일·진입점·작업 디렉터리 확인을 공통화 | `launchers.test.js` |
| 마이그레이션 재실행 | vec_store 제거 후 실제 MariaDB에서 오류 1146으로 중단 | 구 테이블 존재 시에만 벡터 복사 SQL 실행 | `integration/mariadb.test.js` |
| 동기화 CLI 실패 처리 | 벡터·청크 저장 실패에도 종료 코드 0, DB 예외 경로에서는 풀 정리 누락 | 부분 실패 집계·실패 종료 코드·finally 정리 | `integration/mariadb.test.js` |
| 롤백된 삭제 집계 | 커밋 실패로 롤백한 2건을 삭제 성공으로 보고 | 커밋 성공 후에만 성공 건수 누적 | `embed-sync.test.js` |
| 코드 예시 훼손 | 바깥 코드블록 안의 table/chart 예시를 실제 조회 데이터로 바꿈 | 바깥 코드블록 경계를 추적해 독립 참조만 처리 | `chart.test.js` |
| LLM 숫자 ID 반올림 | 숫자 `12345678901234567`을 파싱하면 `12345678901234568`로 바뀌어 실제 Oracle에서 다른 행 조회 | 숫자 토큰이 JS 숫자로 왕복될 때 값이 달라지면 원문 문자열 보존. Oracle 결과와 정밀도 판정 공유 | `llm-openai.test.js`, `oracle/oracle.test.js` |
| 문서 해시의 필드 경계 소실 | 제목 `제목\n첫 문단`·본문 `둘째 문단`을 제목 `제목`·본문 `첫 문단\n둘째 문단`으로 바꿔도 실제 MariaDB에서 옛 청크 유지 | 문서 해시를 JSON 배열로 계산해 제목·본문 경계 보존 | `integration/mariadb.test.js` |

테스트 파일은 모두 `backend/test` 아래에 있다. 재현은 production 함수와 실제 Response/ReadableStream,
드라이버 경계의 제어 가능한 대역, 별도 MariaDB, 별도 Oracle 컨테이너, 실제 시작·종료 프로세스를 사용했다.
롤백 집계는 원래 HEAD 소스를 임시 복사해 새 테스트의 `2 !== 0` 실패를 확인하고 임시 복사본을 제거했다.
숫자 ID 테스트는 수정 전 실제 Oracle에서 `LABEL: 'rounded'`, 수정 후 `LABEL: 'exact'`를 확인했다.
숫자 파서 회귀 테스트는 큰 정수·고정밀 소수·지수·표현 범위 밖 값, 일반 숫자와 문자열,
SSE 조각·일괄 조회, 잘못된 JSON 거부를 함께 검증한다. 문서 해시 테스트는 수정 전 옛 제목·본문이
남는 실패와 수정 후 원문 일치, 그 다음 동기화의 무변경 판정을 확인했다.

검토·검증 범위:

- `server.js`: HTTP 입력·오류·NDJSON 응답, 비동기 로그, 시작·종료와 백그라운드 작업.
- `agent.js`, `context-items.js`, `read-result.js`, `result.js`: 행동 예산·중복 실행·입력 정규화,
  안정적인 자료 ID·근거 보존·확대·숨김·결과 재표시, 사용자 trace의 오류 가림.
- `llm.js`, `llm-openai.js`, `chart.js`, `constants.js`, `numbers.js`: 결정 파싱과 숫자 정밀도, 프롬프트 예산,
  JSON·이스케이프·SSE, 표·차트 주입, 문자열·행·열·응답 상한.
- `sql.js`, `oracle.js`: 주석·리터럴·바인드·읽기 전용 가드, 대상 DB 선택,
  풀 수명, 숫자·날짜·문자열·바이너리·객체 정규화.
- `db.js`, `search.js`, `embedding.js`, `embed-sync.js`, `chunk.js`: 검색 실패와 0건 구분,
  정확 쿼리명 검색, 청크 보충·병합, 임베딩 검증·캐시, 동기화 락·트랜잭션·변경 감지·고아 정리.
- SQL·실행 도구: 스키마와 시드 재적용, 마이그레이션, CLI 실패·정리, PID 소유 판정과 환경 파일 생성.

실제 Oracle에서 단독 CR은 SQL 한 줄 주석을 끝내지 않는다는 점도 확인했다.
이 부분은 결함 후보였으나 기존 가드가 엔진과 일치하므로 수정하지 않았고, 실측 계약을 테스트로 남겼다.
SSE의 여러 data 줄과 줄 끝 규칙은 [WHATWG 표준](https://html.spec.whatwg.org/multipage/server-sent-events.html#event-stream-interpretation)을 대조했다.
SQL 주석은 [Oracle 문서](https://docs.oracle.com/en/database/oracle/oracle-database/21/sqlrf/Comments.html)와 실제 엔진을 함께 확인했다.

테스트 재실행:

```bash
cd backend
npm test                 # 단위·회귀·HTTP·프로세스 테스트
npm run test:integration # 자체 임시 MariaDB의 SQL·동기화·CLI 검증
npm run test:oracle      # 자체 임시 Oracle 컨테이너의 실제 드라이버·조회 검증
npm run test:all         # 위 세 명령을 순서대로 실행
```

MariaDB 통합 테스트는 `mariadb-install-db`, `mariadbd`를 사용한다. 필요하면 `MARIADB_INSTALL_DB`,
`MARIADBD`로 실행 파일을 지정한다. 데이터 디렉터리와 Unix 소켓은 임시 경로에 만들고 TCP 포트는 열지 않는다.
Oracle 테스트에는 실행 중인 Docker와 로컬 `gvenzl/oracle-free:latest` 이미지가 필요하다.
`ORACLE_TEST_IMAGE`로 호환 이미지를 지정할 수 있으며 테스트가 이미지를 자동 다운로드하지는 않는다.
각 테스트는 자기 DB·컨테이너를 생성하고 종료·제거한다. 기존 `.env`의 운영 DB에는 접속하지 않는다.

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, 로컬 Oracle Free 이미지의 Thin 드라이버다.
OCI/Thick 및 Windows 배치 스크립트의 실제 실행은 이번 환경에서 검증하지 않았다.
LLM과 임베딩 API는 프로토콜·실패·스트림 경계를 검증하는 대역을 사용하며 업무 데이터의 모델 정확도 평가는 별개다.

커버리지 계측 실행은 파서 성능 테스트의 고정 시간 상한을 초과했다(계측 시 약 4.7초).
일반 실행에서는 통과하며, 커버리지 계측 실패를 정상 검증 결과로 집계하지 않았다.
커버리지 수치만으로 전체 검토 완료나 결함 부재를 판단하지 않았다.

최종 전체 검증 결과(2026-09-06): `npm run test:all` 종료 코드 0.

| 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 | 434 | 0 | 0 |
| 실제 MariaDB 통합 | 8 | 0 | 0 |
| 실제 Oracle 통합 | 5 | 0 | 0 |
| 합계 | 447 | 0 | 0 |

`git diff --check`와 POSIX 실행 스크립트 구문 검사도 통과했다. 테스트용 Oracle 컨테이너가
남아 있지 않은 것을 확인했다. 확인된 15개 결함 유형은 모두 수정하고 정식 테스트로 남겼다.
이 결과는 위 환경과 검토 범위의 검증 기록이며 모든 운영 환경에서의 결함 부재를 보증하지는 않는다.

문서 해시 형식이 바뀌므로 적용 후 첫 동기화에서는 기존 문서를 한 번 재분할한다.
운영 DB에 대한 배포·동기화 실행은 하지 않았다.

## 추가 전체 검토 (2026-09-06, 기준 커밋 `33c75cc`)

현재 작업 트리의 17개 production 모듈, SQL 5개 파일, 환경 예시·패키지·실행 스크립트를
다시 읽고 `context.md`의 입력·보관·표시·실행 계약과 대조했다. 기존 frontend·README 변경은 보존했다.
수정 전에 전체 테스트 447개(434 + 8 + 5)가 통과하는 것을 현재 환경에서 확인했다.
기존 기록의 통과만으로 검토를 끝내지 않고 아래 두 결함 유형을 추가로 확인했다.

| 결함 | 수정 전 실제 실패 | 수정과 정식 회귀 테스트 |
|---|---|---|
| 문서 판본 혼합 | 검색 적중 1·3번 사이에 개정판 2번이 삽입됨. 재검색은 기존 1·2번과 개정판 3번을 같은 ID로 합침. 확대는 적중 청크 밖의 원문 변경을 감지하지 못함 | 보충 SELECT에 `doc_hash`를 포함하고 검색·재검색·확대에서 해시·본문·제목·청크 수를 비교. 기존 근거와 ID를 유지하고 재검색의 새 절은 별도 항목으로 보관. `search.test.js`, `context-flow.test.js`, `integration/mariadb.test.js` |
| 긴 식별자 충돌 | 공통 접두사를 가진 128자 컬럼명·바인드명이 100자로 잘린 뒤 동일한 키가 되어 첫 값이 사라짐 | 컬럼과 파라미터의 원래 키를 보존. 컬럼이 예산에 안 들어가면 전체 컬럼을 생략. `prompt.test.js`, `oracle/oracle.test.js` |

수정 전에 새 단위·흐름 테스트 5개가 production 함수를 호출해 모두 실패했다.
판본 혼합은 실제 반환 본문에 `기존 절차 1\n기존 절차 2\n개정 절차 3`이 생기는 것으로,
식별자 충돌은 첫 키의 값이 `undefined`가 되고 파라미터 객체에 둘째 값만 남는 것으로 확인했다.
수정 후 이 테스트들을 포함한 관련 테스트 189개가 통과했다.

DB 경계 검증도 정식 테스트에 추가했다. 실제 MariaDB에서 문서 뒤쪽만 바꾸고 동기화한 뒤,
첫 청크 본문이 같아도 해시가 달라지고 이전 판본의 확대가 거부되는 것을 확인했다.
실제 Oracle에서는 128자 바인드 둘과 같은 이름의 컬럼 둘을 조회하고, 넓은 행을 프롬프트 예산으로
줄인 뒤에도 두 이름과 값이 남으며 `read_result`로 다시 읽을 수 있음을 확인했다.
이번 검토의 신규 정식 테스트는 총 7개다.

최신 검증 결과: `cd backend && npm run test:all` 종료 코드 0.

| 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 | 439 | 0 | 0 |
| 실제 MariaDB 통합 | 9 | 0 | 0 |
| 실제 Oracle 통합 | 6 | 0 | 0 |
| 합계 | 454 | 0 | 0 |

`git diff --check`와 backend Bash 스크립트 구문 검사도 통과했다. 임시 Oracle 컨테이너는
테스트 종료 후 남아 있지 않았다. 신규 SQL 스키마 변경이나 운영 DB 작업은 필요하지 않다.
Windows 배치·OCI/Thick의 실환경 실행 및 실제 LLM·임베딩 모델의 업무 정확도는 기존에 기록한
검증 한계와 같다. 확인된 결함은 모두 수정했지만 테스트 통과가 모든 환경의 결함 부재를 증명하지는 않는다.

## 추가 전체 검토 (2026-09-06, 기준 커밋 `8996051`)

시작 시 작업 트리는 깨끗했다. 현재 `backend/src` 17개 모듈, SQL 5개 파일,
환경 예시·패키지·환경 생성 스크립트·POSIX/Windows 실행 스크립트를 다시 검토하고
`context.md`의 검색·근거 보존·행동 제한·결과 표시 계약과 대조했다.
수정 전 `npm run test:all`은 454개(439 + 9 + 6) 모두 통과했다.

새 테스트로 아래 두 production 결함을 확인했다. 단위 테스트에서 먼저 실패를 확인하고,
수정 전 별도 Oracle 컨테이너에서 실제 `runQuery`를 호출해 같은 실패를 재현했다.

| 결함 | 수정 전 재현 | 수정 및 정식 테스트 |
|---|---|---|
| 결과 컬럼 대소문자 충돌 | Oracle은 `"amount"=10`, `"AMOUNT"=100`을 별개 컬럼으로 반환하지만 표 참조는 `AMOUNT=100` 하나만 표시. 백엔드만 수정하면 화면 파서가 좌우 축을 뒤바꿈 | 표·차트와 화면 파서에서 정확한 컬럼명 매칭을 우선함. 기존 대소문자 무시 보완 매칭은 유지. `backend/test/chart.test.js`, `backend/test/oracle/oracle.test.js`, `frontend/test/chart.test.js` |
| 특수 부동소수점 값 소실 | 실제 Oracle의 `BINARY_DOUBLE_INFINITY`, `-BINARY_FLOAT_INFINITY`, `BINARY_DOUBLE_NAN`이 JSON 직렬화 후 모두 `null`이 됨 | 드라이버 경계에서 비유한 숫자를 `Infinity`, `-Infinity`, `NaN` 문자열로 보존. 유한 숫자와 SQL NULL은 기존 타입 유지. `backend/test/oracle.test.js`, `backend/test/oracle/oracle.test.js` |

정식 테스트는 백엔드 4개(단위 2개, 실제 Oracle 2개), 연결된 화면 파서 1개를 추가했다.
화면 파서 변경은 백엔드가 채운 표의 두 컬럼을 각기 올바른 축으로 읽기 위해 필요한 수정이다.
실제 DB 조회 결과 → 표·차트 참조 처리 → 화면 차트 명세의 축과 값까지 검증한다.

요청 항목별 확인:

1. 전체 코드 검토: HTTP/로그/종료, 에이전트 루프와 입력 정규화, LLM JSON/SSE와 프롬프트 예산,
   검색과 청크 병합/판본 보존, 임베딩 동기화/락/트랜잭션, SQL 가드/바인드/Oracle 풀과 값 변환,
   결과 추가 읽기와 표·차트 생성, SQL 설치/시드/마이그레이션, 환경·실행 도구를 검토했다.
2. 재현 후 수정: 새 단위 테스트 2개와 실제 Oracle 테스트 2개의 수정 전 실패를 확인했다.
   컬럼 충돌은 값 10의 소실, 특수 숫자는 SQL NULL과의 혼동으로 확정했다.
3. 정상 동작 검증: 아래 최종 전체 명령이 종료 코드 0이며 실패·취소·건너뜀은 모두 0이다.
4. production 회귀 테스트: 수정한 두 결함을 모두 `npm test` 및 `npm run test:oracle`에 포함했다.

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 백엔드 단위·회귀·HTTP·프로세스 | 441 | 0 | 0 |
| 실제 MariaDB 통합 | 9 | 0 | 0 |
| 실제 Oracle 통합 | 8 | 0 | 0 |
| 백엔드 합계 (`npm run test:all`) | 458 | 0 | 0 |
| 프런트엔드 회귀 (`npm test`) | 122 | 0 | 0 |

연결된 프런트엔드의 `npm run build`, `git diff --check`, backend Bash 구문 검사도 통과했다.
Vite는 기존 큰 청크 경고를 남기지만 빌드는 성공했다. 임시 Oracle 컨테이너가 남지 않았음을 확인했다.
처음의 제한된 샌드박스 실행은 로컬 포트 바인딩 권한 오류로 실패했으며, 권한을 허용한 재실행의
최종 결과만 위에 집계했다. 운영 DB나 기존 `.env`의 접속 대상으로는 테스트하지 않았다.

검증 환경은 Node 25.9.0, MariaDB 12.2.2, oracledb 6.10.0 Thin과 로컬 Oracle Free다.
Windows 배치·OCI/Thick의 실제 실행과 실제 모델의 업무 정확도는 이번 검증에 포함되지 않는다.
새 스키마 변경은 없으며, 확인한 두 결함은 모두 수정했다. 이 기록은 검토와 재현·회귀 검증의
결과이며 모든 입력과 운영 환경에서의 결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-06, 기준 커밋 `574a470`)

시작 시 backend 작업 트리는 깨끗했고 기존 frontend 변경은 보존했다. `backend/src` 17개 모듈,
SQL 5개, 환경 예시·패키지·POSIX/Windows 실행 스크립트를 다시 읽고 `context.md`의 계약과 대조했다.
직전 세 커밋(`33c75cc`·`e8d2fb7`·`574a470`)이 더한 코드를 주 표적으로 삼았다.
검토 전 `npm run test:all`은 471개(453 + 9 + 9) 모두 통과했다.

**이번 검토에서 production 결함은 확인되지 않았다.** 아래 검증 축을 새로 열었고 전부 통과했다.

| 새 검증 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| 답변 후처리 퍼저 | 무작위 답변 × 무작위 조회 행으로 `resolveTableData`→`resolveChartData`를 돌리며, 채운 표의 칸을 화면 파서(`frontend/src/chart.js` splitRow)로 되돌려 원본 셀과 대조 (파이프·역슬래시·개행·이모지·상한 초과) | 결함 없음 |
| 적대적 의존성 퍼저 | `search`/`run`/`decide`/`loadChunks`가 형식이 어긋난 값·이상한 타입·예외를 돌려줄 때 `handleQuestion`이 던지지 않고 이력 줄 수·조회 수 상한, 원문 오류 가림, chat_log 직렬화를 지키는가 (5시드 × 250요청) | 결함 없음 |
| 결정 파서·미리보기 일치 퍼저 | 가짜 SSE 서버로 실제 `openaiDecide`를 태워, 정상 JSON의 글자 보존·초안 비채택·없던 제어문자 미발생·미리보기가 최종 답변의 접두사인가·폐기된 미리보기의 회수 (6시드 × 200) | 결함 없음 |
| embed-sync 실패 통합 | 실제 MariaDB 위에서 행 거부(400)·서버 장애(5xx)·저장 실패·응답 개수 불일치·NaN 벡터·순서 뒤바뀜·GET_LOCK 점유·종료 신호 12가지 | 결함 없음 |
| HTTP 경계 | 실제 서버에 잘못된 본문 9종·1MB 초과·없는 경로·NDJSON 스트림·스트림 중단·동시 12건, chat_log의 거부 사유 기록, SIGTERM 정상 종료 | 결함 없음 |
| 실제 Oracle 왕복 | DATE·TIMESTAMP(3)·TIMESTAMP WITH TIME ZONE·INTERVAL DS/YM의 '읽은 글자를 그대로 바인드로 되돌리기', JSON 컬럼, 세션 NLS 포맷 적용 | 문서·기존 테스트와 일치 |

실제 관리 DB와 실제 임베딩 모델(bge-m3) 위에서 검색·확대·조회·표/차트 채움의 끝에서 끝까지도 확인했다.
전체 테스트는 Node 20(선언된 하한)과 Node 25에서 모두 통과한다.

가장 덜 덮여 있던 `embed-sync.js`(분기 59%)의 계약을 정식 테스트 3개로 못 박았다 — 결함을 고친 것이
아니라 검증한 동작을 회귀 가드로 남긴 것이다(단독 CR 주석 판정을 테스트로 남긴 것과 같은 기준).

| 새 정식 테스트 | 지키는 계약 |
|---|---|
| 임베딩 서버의 행 거부와 서버 장애를 갈라 다룬다 | 400은 배치를 갈라 그 행만 실패로 세고 나머지는 저장한다. 5xx는 회차를 접되 배치 한 번만 시도하고(죽은 서버에 행마다 매달리면 회차가 60초 × 행 수만큼 늘어진다) 아무것도 실패로 세지 않으며, 서버가 돌아오면 남긴 행을 그대로 이어받는다 |
| 임베딩 응답 순서가 뒤바뀌어도 index로 짝짓는다 | 위치가 아니라 `index`로 정렬해 텍스트와 벡터가 어긋나지 않는다. 정렬을 빼면 정합성 검사가 배치를 거부해 행 단위 재시도로 물러나므로, 임베딩 왕복 수로 그 퇴화를 잡는다 |

세 테스트가 실제 회귀 가드인지 변이 검사로 확인했다: `retriable` 판정, `index` 정렬, 거부된 배치의
행 단위 분할, 재시도 가능 실패의 조기 반환을 각각 되돌리자 4건 모두 해당 테스트가 실패했다.

확인했으나 결함이 아닌 것(근거를 남겨 다시 파지 않게 한다):

- `\n` 뒤에 `eq`로 시작하는 줄이 오면 줄바꿈이 리터럴 `\n`으로 남는다 — `N_COMMAND_TAILS`가 주석에
  명시한 트레이드오프이고, 손해는 '수식 한 줄이 이상하게 보인다'로 한정된다.
- 닫히지 않은 JSON 문자열이 다음 객체의 여는 중괄호를 삼키면 미리보기가 최종 파서와 다른 후보를
  보인다(퍼징 82건 중 3건). 미리보기는 선언된 대로 best-effort이고 `done`이 최종 답으로 바꿔 끼운다 —
  폐기된 답이 화면에 남는 경우는 한 번도 없었다.
- `TIMESTAMP(6)`의 밀리초 아래 값은 JS `Date`가 담지 못해 되돌린 바인드가 0건이 된다. 이 커밋 이전
  (초 단위)에도 같았고, 등록 SQL의 `TO_CHAR`가 해법이며 `oracle/oracle.test.js`가 이미 그 계약을 잡는다.

검증 환경은 macOS, Node 25.9.0(및 Node 20.20.2), MariaDB 12.2.2, Oracle Free 26ai(Thin), bge-m3다.
실제 OpenRouter 응답과 Windows 배치 스크립트의 실행은 이번에도 검증 범위 밖이다.

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 | 453 | 0 | 0 |
| 실제 MariaDB 통합 | 11 | 0 | 0 |
| 실제 Oracle 통합 | 9 | 0 | 0 |
| 합계 (`npm run test:all`) | 473 | 0 | 0 |

production 소스는 한 줄도 바꾸지 않았다 — 이번 검토에서 고칠 결함이 나오지 않았기 때문이다.
이 기록은 위 환경과 검토 범위의 결과이며 모든 입력과 운영 환경에서의 결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-07, 기준 커밋 `ee74f90`)

시작 시 작업 트리는 깨끗했다. `backend/src` 17개 모듈, SQL 5개, 환경 예시·패키지·POSIX/Windows 실행
스크립트를 다시 읽었다. 앞선 회차들이 지식·처리방법 경로를 무작위 퍼저와 전수 열거로 훑었으므로,
이번에는 각도를 **응답 경로 밖의 수명 관리와 '가드보다 뒤에서 도는 변환'** 으로 잡았다.
검토 전 `npm run test:all`은 473 + 11 + 9 = 493개 모두 통과했다.

**production 결함 3건을 확인해 고쳤다.** 셋 다 수정 전 실패를 재현한 뒤 고쳤고, 셋 다 정식 테스트로 남겼다.

| 결함 | 재현 | 수정과 정식 테스트 |
|---|---|---|
| 임베딩 예열이 종료 신호를 받지 않아 정상 종료가 강제 종료가 된다 | 응답하지 않는 임베딩 서버를 띄우고 실제 서버에 SIGTERM — **10.0초 뒤 종료 코드 1**, `[shutdown] cleanup timed out`. 강제 타이머는 `process.exit`이라 `closePool()`·`closeOraclePools()`가 실행되지 않는다(관리 DB에 끊긴 커넥션, 조회 DB에 세션이 남는다). 모델 콜드 로드가 정상적으로 30초+ 걸리므로 그 창은 기동 직후 재배포와 정확히 겹친다 | `warmUpEmbedding(signal)`이 종료 신호를 받아 `embed()`에 넘긴다. 신호는 embed-sync가 이미 갖고 있던 그것을 `shutdownSignal()`로 내주고 배선은 server.js가 한다(search.js가 embed-sync를 import하면 순환이다). 신호로 끊긴 호출은 경고를 남기지 않는다. 수정 뒤 **50ms/코드 0**. `backend/test/search.test.js`(단위), `backend/test/server.test.js`(프로세스 경계 — server.js가 신호를 넘기지 않는 회귀는 여기서만 드러난다) |
| 답변 후처리가 답변을 통째로 비운다 | 본문이 빈 ` ```table ` 블록 하나만 든 답변에서 `resolveTableData`가 펜스를 벗기면 남는 글자가 없다 → **답변이 빈 문자열**. 빈 답변은 `llm-openai.js toDecision`과 `agent.js answerOf`가 두 번 막기로 한 것인데, 후처리는 그 둘보다 뒤라 두 가드를 모두 지난 답변을 이 자리에서 비웠다. 화면에는 빈 말풍선이 뜨고 chat_log에는 답한 것으로 기록된다 | 본문이 비었으면 '다른 용도로 쓴 펜스'가 아니므로 벗기지 않고, 설정 줄만 있는 블록과 같은 안내로 바꾼다. `backend/test/chart.test.js`(그 자리), `backend/test/agent.test.js`(경계 계약 — 다른 후처리가 같은 일을 해도 잡힌다) |
| 홑 CR로 줄을 바꾼 답변에서 절단 보호 둘이 함께 꺼진다 | `clipAnswer`의 두 자리만 `'\n'`을 보고 있었다(저장소의 다른 자리는 전부 홑 CR도 줄 끝으로 센다 — `constants.indentLines`, `chart.js` fencedBlocks·splitBlock, `llm.js` cell, 프런트 파서). ① 마지막 줄바꿈을 못 찾아 차트 블록 안의 표 행이 `\| 2026-`처럼 반만 남고(프런트는 그 조각을 값으로 그린다) ② 여는 펜스를 줄로 보지 못해 열린 코드블록을 닫지 못해 안내 문장이 코드블록에 삼켜진다. 닿는 경로는 폴백 답변이다 — 조회 결과와 등록 원문 그대로의 지식 본문을 이어 붙여 이 상한(70,000자)을 실제로 넘고, CR은 그 등록 원문에서 온다 | 줄 끝 판정을 저장소의 나머지와 같은 규칙(`/\r\n?|\n/`)으로 맞추고 두 자리가 그 하나를 쓴다. `backend/test/llm.test.js` |

발견 기법은 둘이다. **① 형제 갈래 비대칭** — `embed()`를 부르는 셋 중 embed-sync의 둘만 종료 신호를
넘기고 예열만 빠져 있었다(11차가 `applyExpand`에서 쓴 것과 같은 각도를 수명 관리에 겨눴다).
**② 가드보다 뒤에서 도는 변환** — '빈 답변은 내보내지 않는다'를 두 곳이 막는데 후처리는 그 뒤였다.
같은 눈으로 훑은 나머지(크기 상한·짝 잃은 서로게이트·잘린 값 가드·바인드 가드)는 뒤따르는 변환이
그 판정을 되돌리지 않는 것을 확인했다.

새로 연 검증 축(전부 통과):

| 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| 적대적 세션 소크 | 형식 어긋난 결정 8종 × 의존성 실패(검색 15% 불가·5% 예외, 조회 12% 실패, 결정 5% 예외)로 `handleQuestion`을 돌리며 던지지 않음·이력 줄 수·검색/조회 상한·`trace` 직렬화·`clientTrace` 순번 오름차순·원문 오류와 hint의 화면 유출을 본다. 5시드 × 3,000 = **15,000세션** | 결함 1건(빈 답변) 확인 후 위반 0 |
| 이력 몫 불변식 | `MAX_HISTORY_ROWS` 줄이 '어떤 조합으로 오든' 전부 실리는가 — 최악 크기의 결과·오류·안내·검색·청구 줄을 무작위로 섞어 3,000회 렌더 | 생략 0건, 최대 프롬프트 34,943자 |
| 청크 왕복 | `splitContent` → 이어 붙이기가 원문을 정확히 복원하는가. LF/CRLF/**홑 CR** × 펜스·제목·빈 줄·같은 글자 200자 반복·이모지 코퍼스 4,000건 | 어긋남 0 |
| 시간대 | `TZ=UTC`, `TZ=America/Sao_Paulo`에서 전체 단위 검사 | 각 473개 통과 |
| 고전 결함 패턴 기계 검색 | 비교 함수 없는 `sort`, radix 없는 `parseInt`, 느슨한 동등, `for…in`, 모델이 정한 키의 직접 인덱싱 | 해당 없음(전부 의도된 `== null`·`ownProp` 경유) |
| 환경변수 대조 | 코드가 읽는 이름 ↔ `.env.example` 키 | 어긋남 없음 |

확인했으나 결함이 아닌 것(근거를 남겨 다시 파지 않게 한다):

- `agent.js cellShownBefore`도 줄 시작을 `'\n'`으로만 찾지만 해가 없다 — 뒤로 훑어 가장 가까운
  이스케이프되지 않은 `|`를 찾으므로 표 행에서는 언제나 줄 시작에 닿기 전에 올바른 칸 경계를 만난다.
- `sql.js`의 한 줄 주석이 홑 CR에서 끝나지 않는 것은 실제 Oracle과 일치한다
  (`test/oracle/oracle.test.js`의 '주석 줄 끝 판정이 Oracle과 일치한다'가 이미 그 계약을 잡는다).
- `db.js`의 `RESERVED_FOR_SYNC`(1)는 동기화가 실제로 겹쳐 쥐는 몫(락 커넥션 + 청크 트랜잭션 = 2)보다
  작지만, 요청당 여유 한 칸(`CONNS_PER_REQUEST` 5 vs 실측 피크 4)이 그 이상을 덮는다.
- `.bat` 실행 스크립트는 창 제목으로 프로세스를 찾으므로 같은 저장소의 다른 체크아웃과 구분하지 못한다
  (POSIX 쪽은 `process.sh`가 작업 디렉터리로 구분한다). Windows에는 그에 상응하는 값싼 판정이 없다.
- 관리 DB가 닿지 않는 환경에서 주기 작업이 커넥터의 획득 상한(10초)까지 매달리는 것은 커넥터 기본값이다
  (`server.test.js`의 종료 검사가 그 둘이 끝난 뒤에 SIGTERM을 보내는 이유).

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, Oracle Free(gvenzl/oracle-free, 임시 컨테이너), 임베딩은
가짜 서버다. 실제 OpenRouter 응답과 Windows 배치 스크립트의 실행은 이번에도 검증 범위 밖이다.
임시 Oracle 컨테이너는 테스트 종료 후 남지 않았다. 새 스키마 변경은 없다.

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 (`npm test`) | 478 | 0 | 0 |
| 실제 MariaDB 통합 | 11 | 0 | 0 |
| 실제 Oracle 통합 | 9 | 0 | 0 |
| 백엔드 합계 (`npm run test:all`) | 498 | 0 | 0 |
| 프런트엔드 회귀 (`npm test`) | 133 | 0 | 0 |

`git diff --check`와 backend Bash 스크립트 구문 검사도 통과했다. 새로 더한 정식 테스트는 5개다
(단위 4 + 프로세스 1). 이 기록은 위 환경과 검토 범위의 결과이며 모든 입력과 운영 환경에서의
결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-07, 기준 커밋 `4ef1880`)

시작 시 작업 트리는 깨끗했다. 앞선 회차들이 지식·처리방법 경로를 무작위 퍼저와 전수 열거로,
직전 회차가 수명 관리와 '가드 뒤의 변환'을 훑었으므로, 이번에는 각도를 **① 문서가 운영자에게
시키는 것이 실제로 도는가 ② 변이 테스트로 '어떤 판정이 아무 테스트도 보지 않는가'를 지도로 만든 뒤
그중 결과가 큰 것을 직접 재기**로 잡았다. 검토 전 `npm run test:all`은 478 + 11 + 9 = 498개 통과했다.

**production 결함 1건을 확인해 고쳤다.** 수정 전 실패를 재현한 뒤 고쳤고 정식 테스트로 남겼다.

| 결함 | 재현 | 수정과 정식 테스트 |
|---|---|---|
| `start_all.sh`가 임베딩 모델 이름을 셸 환경에서만 읽는다 | 백엔드는 그 값을 `backend/.env`에서 읽는데(dotenv) 스크립트는 셸 환경만 본다. 문서가 시키는 대로(README '임베딩 모델 교체', `setup/bge-m3/README.md`) .env에서 모델을 바꾸면, 스크립트는 옛 이름으로 `ollama list`를 보고 **"임베딩 모델 bge-m3 준비됨"이라 알리고 새 모델은 받지 않는다.** 그 상태의 증상은 '모든 검색이 검색 불가'인데(검색은 벡터 단일 경로) 스크립트는 성공을 보고하므로, 이 검사가 막겠다고 스스로 적어둔 결과가 검사를 통과한 채로 난다 | 모델 이름을 백엔드가 읽는 곳에서 읽는다 — 셸 환경 > `backend/.env` > 기본값(dotenv와 같은 우선순위, 파서도 Node의 `--env-file`로 같은 부류). .env·node가 없으면 종전대로 기본값. `backend/test/launchers.test.js` 2개(대역 인프라 위에서 실제 스크립트를 돌려 어느 이름으로 판정·pull하는지, 그리고 새 클론의 기본값) |

**회귀 가드 1개 추가(결함 아님).** 변이 테스트가 `applyExpand`의 세 갈래에서 `row.expanded = true`를
각각 지워도 아무 테스트도 잡지 못했다 — 그 표시는 그 스텝에서는 아무 차이도 만들지 않고, '뒤이은
검색'에서만 드러난다(새 결과가 `mergeFront`의 앞자리를 차지해 청구한 자료가 뒤로 밀리고, 문서별 상한과
섹션 예산을 뒤에서 받게 되어 모델이 왕복 하나를 들여 부른 본문이 다음 스텝에서 잘린다).
동작 자체는 옳았으므로 고친 것이 없고, 세 갈래를 모두 지나는 검사를 `context-flow.test.js`에 남겼다 —
변이 셋 다 이 검사가 잡는다.

새로 연 검증 축(전부 통과):

| 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| README의 chat_log 분석 SQL | 운영자가 문서대로 실행하는 5개 질의(못 답한 질문·거부 추이·등록 후보·검색 없이 답한 질문·느린 요청)를 실제 MariaDB에서 9가지 trace 표본에 돌려, 각 표본이 후보에 들어야 하는지를 대조 | 어긋남 없음. `JSON_VALUE`가 JSON null과 없는 키를 모두 SQL NULL로 준다는 전제(README의 `COALESCE(...,0)`·`IS NULL`)도 실측 확인 |
| 변이 테스트 (10차가 훑지 않은 파일) | `result.js`·`read-result.js`·`chart.js`·`llm.js`·`agent.js` 총 422개 변이. 살아남은 것을 전부 갈라 원본이 옳은지 확인 | 결함 0. 생존 174개는 등가 변이(`??`↔`\|\|`가 배열·객체에서 같은 값), 도달 불가, 검증 공백 |
| 청구 고정의 지속 | 위 회귀 가드 — 확대·포화·숨김 복구 세 갈래가 뒤이은 검색 뒤에도 앞자리와 본문을 지키는가 | 지켜진다 |

확인했으나 결함이 아닌 것(근거를 남겨 다시 파지 않게 한다):

- 백엔드 `chart.js fencedBlocks`의 펜스 꼬리 판정이 `.trim()`이라 markdown(스페이스·탭)보다 넓다 —
  프런트는 16차에서 `[ \t]`로 좁혔다. 그러나 이쪽은 **가리는 쪽이 아니라 다시 쓰는 쪽**이라 넓은 판정이
  어긋남이 아니라 복구가 된다: 인식한 블록은 깨끗한 펜스로 바꿔 내보내므로 화면과 이력이 갈라지지 않는다.
  좁히면 반대로 NBSP가 붙은 닫는 펜스의 `data:` 참조가 채워지지 않은 채 남아 사용자가 설정 줄을 코드로 본다.
- `read_result.cols`와 `run_queries.queries`는 목록만 받는다(`search.targets`·`expand.ids`·`drop`은 하나도 받는다).
  형식 경계가 받아 주는 폭이 갈리지만, `cols`는 오류 문구가 형식을 되돌려 주고 `queries`는 한 항목짜리 배치를
  `run_query`로 쓰면 되는 자리라 관측된 실패가 없다 — 관측되면 `ids`와 같은 규칙으로 넓힐 것.
- `llm.js clipAnswer`의 `openFence`도 `.trim()`을 쓰지만, 이 자리는 '펜스를 닫아 주는' 쪽이라 넓은 판정이
  안전한 방향이다(닫지 않는 쪽이 손해다).
- `analysisCache`·`embedCache`·`lastWarned`·`pools`·`pendingLogWrites` 등 모듈 수명 상태의 증가 경계와,
  `effortAccepted`·`streamOptionsAccepted`가 한 번만 꺼지는 것(문서화된 트레이드오프).
- 모든 `catch`가 남기는 상태(트랜잭션 롤백·커넥션 반납·캐시 미오염) 전수 확인.

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, Oracle Free(임시 컨테이너), 임베딩은 대역이다.
실제 OpenRouter 응답과 Windows 배치 스크립트의 실행은 이번에도 검증 범위 밖이다.
임시 Oracle 컨테이너는 남지 않았고 새 스키마 변경은 없다.

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 (`npm test`) | 481 | 0 | 0 |
| 실제 MariaDB 통합 | 11 | 0 | 0 |
| 실제 Oracle 통합 | 9 | 0 | 0 |
| 백엔드 합계 (`npm run test:all`) | 501 | 0 | 0 |
| 프런트엔드 회귀 (`npm test`) | 133 | 0 | 0 |

`git diff --check`와 Bash 스크립트 구문 검사도 통과했다. 새로 더한 정식 테스트는 3개다
(실행 스크립트 2 + 회귀 가드 1). `backend/src`는 한 줄도 바뀌지 않았다 — 이번 회차에서 확인한
production 결함이 실행 스크립트 쪽 하나였기 때문이다. 이 기록은 위 환경과 검토 범위의 결과이며
모든 입력과 운영 환경에서의 결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-07, 기준 커밋 `34cee38`)

시작 시 작업 트리는 깨끗했다. 각도는 **① 운영자가 DB에 직접 넣는 등록 값의 빈/이상 값을 실행 경계가
어떻게 다루는가**(12차의 '적대적 등록 데이터'가 `query_registry`만 훑고 `target_db`는 빼놓았다)와
**② 아직 변이 검사를 하지 않은 파일**(10차 3개·20차 5개를 뺀 나머지)로 잡았다.
검토 전 `npm run test:all`은 481 + 11 + 9 = 501개 통과했다.

**production 결함 1건을 확인해 고쳤다.** 실 Oracle로 재현한 뒤 고쳤고 정식 테스트로 남겼다.

| 결함 | 재현 | 수정과 정식 테스트 |
|---|---|---|
| `target_db`의 접속 값이 비어 있어도 조회 DB에 그대로 접속을 시도한다 | `resolvePassword`는 `ENV:변수명`이 가리키는 변수가 비면 접속을 막고 그 근거까지 적어 두었다("시도하면 DB는 ORA-01017을 돌려주고 운영자는 설정 누락이 아니라 자격증명이 틀렸다고 읽는다. 빈 비밀번호 로그인이 매 조회마다 반복되면 공용 조회 계정이 FAILED_LOGIN_ATTEMPTS에 걸려 잠긴다"). **평문으로 등록한 값에는 그 판정이 없었다.** 실 Oracle(FREEPDB1)로 실측: `{user:'APP_USER', password:''}`와 `{user:'', password:''}` 모두 클라이언트에서 막히지 않고 **서버가 ORA-01017을 돌려준다** — 즉 진짜 로그인 시도이고, 그 주석이 막겠다고 한 계정 잠금이 평문 등록에서 그대로 난다. `connection_info`가 비면 더 나쁘다: Thick(oci)에서 빈 접속 문자열은 클라이언트의 기본 DB(`ORACLE_SID`/`TWO_TASK`)로 간다(실측 ORA-12162 — 그 변수가 있는 호스트에서는 실제로 붙는다). 그러면 등록과 **다른 DB의 행이 등록된 이름표를 달고** 이력·답변·trace로 나가는데 어디에도 그 사실이 남지 않는다 | 실행 경계에서 `connection_info`·`db_user`·`db_password`가 비면 접속하지 않고 ENV: 갈래와 같은 문구·같은 표시(`safe` + `wastedStep`)로 실패한다. 판정 기준도 그쪽과 같은 falsy다(공백만 든 값은 양쪽 다 통과). 외부 인증은 깨지지 않는다 — 그 경로는 속성을 '비우는' 것이 아니라 '주지 않는' 것이고, NULL을 그대로 넘기는 종전 동작은 어차피 NJS-007로 죽었다(실측). `backend/test/oracle-pool.test.js` |

**회귀 가드 2개 추가(결함 아님 — 검증한 동작을 못 박은 것).** 둘 다 '형제는 검사가 있는데 이쪽만 없던' 자리다.

| 가드 | 왜 |
|---|---|
| `ENV:` 비밀번호의 해석과 빈 값 거부 (`oracle-pool.test.js`) | 위 수정의 형제 갈래. 판정은 처음부터 있었지만 검사가 없어, 변이로 `typeof stored === 'string'`을 뒤집거나 `ENV_PREFIX`의 `i` 플래그를 떼도 아무 검사가 잡지 못했다. 둘이 같은 문구·같은 표시로 실패해야 운영자가 '평문이 비었나 ENV가 비었나'를 로그에서 가른다 |
| `reasoning_effort`를 모르는 서버에서의 재시도 성공 (`llm-openai.test.js`) | 소스는 `stream_options`와 이 파라미터를 같은 방식으로 다루는데(400 본문에 이름이 보이면 그 요청부터 빼고 바로 이어지는 재시도가 성공한다) 검사는 `stream_options`에만 있었다. 깨지면 그 엔드포인트의 **모든 질문이 'LLM 호출 실패'** 가 되고, 이 표시가 존재하는 이유가 정확히 그것을 막는 것이다. 검사는 파일 맨 끝에 둔다 — 표시는 프로세스 수명 동안 유지되므로 앞의 기본값 검사보다 먼저 돌면 그쪽이 깨진다 |

새로 연 검증 축(전부 통과):

| 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| `target_db` 적대적 등록 데이터 | 빈·NULL `db_user`/`db_password`/`connection_info`, `ENV:` 미설정·빈 값·대소문자 표기 | 결함 1건 확인(위) |
| 실 Oracle 자격증명 실측 | 빈/NULL 비밀번호와 빈 접속 문자열이 클라이언트에서 막히는가, 서버까지 가는가 — thin과 Thick(oci) 양쪽 | thin: ORA-01017·NJS-125, Thick: ORA-01017·ORA-12162 |
| 변이 테스트 (10·20차가 훑지 않은 파일) | `sql.js`(51)·`oracle.js`(111)·`llm-openai.js`(508)·`numbers.js`(10)·`constants.js`(65) = **745개**. 생존을 전부 갈라 원본이 옳은지 확인 | 결함 0. 생존 250개는 등가 변이·도달 불가·검증 공백 |

확인했으나 결함이 아닌 것(근거를 남겨 다시 파지 않게 한다):

- `db_type`이 비면 oracle로 본다(`target.db_type &&`) — 컬럼에 `DEFAULT 'oracle'`이 있고, 빈 값을
  거부하면 기본값을 쓴 등록이 통째로 막힌다.
- 새 가드를 `ORACLE_MOCK` 경로 앞으로 올리지 않았다 — `resolveTargetDb`와 달리 이 판정은 관리 DB를
  읽어야 하고(`loadTargetDb`), mock의 존재 이유가 '접속 없이 돈다'이다.
- `numbers.js decimalParts`의 부호 판정을 뒤집어도 결과가 같다 — 비교하는 두 값이 같은 함수를 지난다(등가 변이).
- `constants.js safeError`의 hint 스프레드는 `test/oracle.test.js`가 이미 잡는다(변이 회차의 테스트 집합에
  그 파일이 없어 생존으로 보였을 뿐이다).
- 400이 아닌 상태 코드(예: FastAPI 기본 422)로 파라미터를 거절하는 서버에서는 위 '한 번 배우고 뺀다'가
  발동하지 않는다 — 그 구성에서는 `LLM_REASONING_EFFORT=off`가 문서화된 손잡이다.

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, Oracle Free 23ai(로컬 컨테이너 + 테스트가 만드는 임시
컨테이너), Instant Client 23.26(Thick 실측). 실제 OpenRouter 응답과 Windows 배치 스크립트의 실행은
이번에도 검증 범위 밖이다. 임시 Oracle 컨테이너는 남지 않았고 새 스키마 변경은 없다.

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 (`npm test`) | 484 | 0 | 0 |
| 실제 MariaDB 통합 | 11 | 0 | 0 |
| 실제 Oracle 통합 | 9 | 0 | 0 |
| 백엔드 합계 (`npm run test:all`) | 504 | 0 | 0 |
| 프런트엔드 회귀 (`npm test`) | 133 | 0 | 0 |

`git diff --check`와 Bash 스크립트 구문 검사도 통과했다. 새로 더한 정식 테스트는 3개다
(결함 재현 1 + 회귀 가드 2). 이 기록은 위 환경과 검토 범위의 결과이며 모든 입력과 운영 환경에서의
결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-07, 기준 커밋 `1f659f1`)

시작 시 작업 트리는 깨끗했다. 각도는 **'우리가 쓴 글자를 실제 렌더러가 어떻게 읽는가'** 로 잡았다.
앞선 회차가 판 축(퍼저·변이 테스트·실 MariaDB·실 Oracle·가짜 SSE LLM)은 전부 *우리 코드끼리의 약속*을
쟀는데, 서버가 답변에 채워 넣는 표는 최종 판정자가 우리가 아니다 — 화면에서 그것을 읽는 것은
remark-gfm이다. 프런트의 실물 파서(`unified`+`remark-gfm`, `react-markdown`+`react-dom/server`)에
서버 출력을 그대로 걸어 대조하자 첫 시도에서 결함이 나왔다. 함께, 아직 변이 검사를 하지 않은
마지막 네 파일(`db`·`embedding`·`embed-sync`·`server`)을 훑었다.
검토 전 `npm test` 484 + MariaDB 통합 11개가 통과했다(Oracle 통합은 Docker가 내려가 있어 못 돌렸다).

**production 결함 2건을 확인해 고쳤다.** 둘 다 같은 경계(`backend/src/chart.js`가 답변에 표를 채워
넣는 자리)이고, 둘 다 실물 파서로 재현한 뒤 고쳤으며 정식 테스트로 남겼다.

| 결함 | 재현 | 수정과 정식 테스트 |
|---|---|---|
| 채운 칸의 값이 markdown으로 해석돼 **사용자가 DB의 값과 다른 글자를 읽는다** | 칸은 GFM 표의 **인라인 문맥**이라 값에 든 강조·코드·링크·취소선·원시 HTML·엔터티 표기가 그대로 산다. `escapeCell`은 `\`와 `|`만 막고 있었다 — 그 둘은 *열을 미는* 글자라 눈에 띄었지만, 나머지는 열을 밀지 않고 값만 조용히 바꾼다. 실무형 값 47건을 실물 렌더러에 걸어 7건이 훼손됐다: 치수 `10*20*30`→`102030`, `~미사용~`→`미사용`, `__init__`→`init`, `&amp;`→`&`, `` 노트: `code` ``→`노트: code`, `[확인](http://x)`→링크, `![i](u)`→그림 표기. 사용자는 그 값을 다음 질문에 옮겨 적으므로 이 저장소가 가장 나쁘게 보는 '조용한 오답'이다 | **그 칸에서 실제로 구성요소가 될 수 있을 때만** 막는다 — 구성요소는 전부 짝이 있어야 성립하므로 판정이 정확하다(구분자 **런** 2개 이상, `[`보다 뒤의 `]`, `<`보다 뒤의 `>`, `&이름;`). `_`만 규칙이 하나 더 붙는다: 낱말 가운데의 `_`는 강조를 열지도 닫지도 못하므로(CommonMark intraword 예외) 세지 않는다 — 덕분에 `BATCH_JOB_STATUS`·`order_id`·`METRIC__FIRST`가 지금까지와 글자 하나 다르지 않다. '늘 막는' 방식을 쓰지 않은 이유가 그것이다: 이 글자는 이력으로 되돌아가 모델이 다시 읽는 자리다. 되돌리는 두 곳(`agent.js unescapeCell`, `frontend/src/chart.js splitRow`)이 같은 목록을 보고, `llm.js cell`은 사본 대신 `escapeCell`을 그대로 쓴다. `backend/test/chart.test.js`, `frontend/test/chart.test.js`(실물 렌더러) |
| 펜스를 표로 바꾸면서 **블록 경계를 잃어 옆의 표·문장을 삼킨다** | 펜스는 닫는 줄이 곧 경계지만 GFM 표는 빈 줄이나 다른 블록이 와야 끝나고 그 전까지 오는 줄을 자기 행으로 삼킨다. 채우기 '전'에는 멀쩡하던 답변이 채운 뒤에만 무너진다 — 이 치환이 스스로 만드는 실패다. 실물 파싱으로 셋을 확인했다: ① 표 블록 둘이 빈 줄 없이 이어지면 뒤 표의 머리글과 `---` 줄이 앞 표의 데이터 행이 된다 ② 모델이 손으로 쓴 1열 표 바로 뒤에 오면 흡수되어 2열 결과의 `STATUS` 값이 **화면에서 통째로 사라진다** ③ 표 바로 뒤의 설명 문장이 표의 마지막 행이 된다 | `replaceBlocks`가 바꾼 블록의 앞뒤에 빈 줄이 없으면 넣는다(바꾸지 않은 블록은 원문 그대로 둔다). 안내 문장(`_표를 채우지 못했습니다…_`)도 같은 규칙을 받는다 — 앞이 표면 그 문장이 표의 행이 되기 때문이다. `backend/test/chart.test.js`, `frontend/test/chart.test.js` |

**제 수정을 2차로 검토해 스스로 만든 문제 둘을 다시 고쳤다**(이 저장소의 '수정이 새 버그를 낳는다'
규칙 그대로다).

- 블록 경계 판정이 블록마다 `text.slice(at)`와 `$` 앵커 정규식을 돌려 **'블록 수 × 답변 길이'** 가
  됐다. 실측: 빈 표 블록 4,000개(84KB)에 75ms, 길이를 두 배로 하면 아홉 배. 답변 상한 안에 이런 블록
  수천 개가 들어가고 그 시간은 동기 작업이라 그동안 처리 중인 모든 요청이 함께 멈춘다. 뒤쪽은 sticky
  정규식으로, 앞쪽은 꼬리 4KB 버퍼로 선형화했다(12.6ms). **이어 붙인 문자열을 인덱스로 읽으면 V8이
  매번 평탄화한다**는 옛 교훈이 그대로 재현된 자리다 — 선형성 시험을 정식 테스트로 남겼다.
- 엔터티 판정의 `ENTITY_AT.test(s.slice(i))`도 같은 이유로 sticky 정규식으로 바꿨다.

**회귀 가드 3개 추가(결함 아님 — 변이가 살아남던 자리를 못 박은 것).**

| 가드 | 왜 |
|---|---|
| 거부의 상태 코드 400·404·413·200 (`server.test.js`) | 기존 검사는 '4xx/5xx 중 하나'만 재서 400·404·500을 서로 바꿔도 아무 검사가 잡지 못했다(변이로 확인). 서버는 '클라이언트가 고칠 수 있는 요청'과 '없는 경로'와 '서버 버그'를 코드로 갈라 보내기로 했고(오류 핸들러 주석: 전부 400으로 뭉개면 원인 분류가 뒤집힌다), 감시자·프록시·접속 로그는 그 코드만 본다 |
| 동기화 CLI의 성공 종료 코드 0 (`integration/mariadb.test.js`) | 실패만 단언하고 있어 '언제나 1을 돌려주는' 회귀가 그대로 통과했다(변이로 확인). 프로비저닝 스크립트가 이 코드로 성공을 판정하므로, 늘 1이면 정상 배포가 매번 실패로 기록된다 |
| `knowledge_chunk.chunk_no`는 1부터 이어진다 (`integration/mariadb.test.js`) | 0부터 매겨도 어떤 검사도 잡지 못했다(변이로 확인). 0-based가 되면 `buildItems`의 `closed(no) → no < 1`과 확대 창(`growItem`의 `Math.max(1, …)`)이 첫 조각을 문서 밖으로 보고 그 조각을 영영 읽지 못하는데, 증상은 '(확대 가능) 표시가 사라지지 않는다'뿐이다 |

새로 연 검증 축(전부 통과):

| 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| 실물 markdown 렌더러 왕복 | 서버가 채운 표의 칸을 `remark-gfm`으로 파싱해 조회 결과 원문과 대조. 무작위 적대 셀 12,500건 + 실무형 47건 + 배치 위치 10종 | 결함 2건 확인(위). 수정 뒤 어긋남 0 |
| 잘린 값 가드 회귀 | 이스케이프 목록이 늘어도 `clippedCopyDetector`가 칸에 보인 앞부분을 알아보는가 (수정 전/후 각 1,500건) | 양쪽 0건 놓침 |
| 변이 테스트 (마지막 네 파일) | `db.js`(32)·`embedding.js`(47)·`embed-sync.js`(101)·`server.js`(98) = **278개**. 이로써 17개 src 전부가 변이 검사를 지났다 | 결함 0. 생존 170개는 설정값·등가 변이·검증 공백(그중 셋을 위 가드로 덮었다) |

확인했으나 결함이 아닌 것(근거를 남겨 다시 파지 않게 한다):

- 인용 안의 펜스(`> ```table`)는 참조가 채워지지 않는다 — 백엔드와 프런트의 펜스 판정이 둘 다 `>`를
  모르므로 대칭이고, 화면과 이력이 어긋나지 않는다.
- `renderAnswer`의 제목·쿼리명·오류 문구가 markdown으로 해석되는 것 — 그 자리는 데이터가 아니라
  라벨이고, 폴백 답변의 지식 본문은 원래 markdown이다.
- 이스케이프된 칸이 다음 턴 이력으로 모델에게 가는 것 — `\|`가 이미 그랬고(같은 결정의 연장),
  시스템 프롬프트가 바인드 값의 출처를 '사용자 질문 또는 실행 이력'으로 못 박는다(이력의 행은 원문 JSON이다).
- mariadb 커넥터의 `queryTimeout`은 실제로 커넥션마다 `SET max_statement_time`을 건다
  (`node_modules/mariadb/lib/connection.js` 실물 확인 — 3.5.3).
- Node 20 하한(package.json engines) 밖의 API 사용은 없다(`Object.groupBy`·`Promise.withResolvers`·
  `findLast`·`toSorted`·`Set` 집합 연산·`RegExp.escape`·`AbortSignal.any` 기계 검색).

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, remark-gfm 4.x / react-markdown 10.x(프런트 의존성 실물).
실제 OpenRouter 응답과 Windows 배치 스크립트는 이번에도 검증 범위 밖이고, Oracle 통합은 Docker가
내려가 있어 돌리지 못했다 — 대신 그 테스트가 표·차트에 대해 단언하는 것을 같은 행 모양으로 재현해
통과를 확인했다(수정이 그 경로를 건드리지 않는다).

| 최종 검증 | 통과 | 실패 | 건너뜀 |
|---|---:|---:|---:|
| 단위·회귀·HTTP·프로세스 (`npm test`) | 488 | 0 | 0 |
| 실제 MariaDB 통합 | 12 | 0 | 0 |
| 실제 Oracle 통합 | — | — | 9 (Docker 미기동) |
| 프런트엔드 회귀 (`npm test`) | 136 | 0 | 0 |

새로 더한 정식 테스트는 9개다(결함 재현 6 + 회귀 가드 3). 이 기록은 위 환경과 검토 범위의 결과이며
모든 입력과 운영 환경에서의 결함 부재를 보증하지는 않는다.

## 추가 전체 검토 (2026-09-07, 기준 커밋 `8fa97fd`)

앞 회차가 연 축("우리가 쓴 글자를 실제 렌더러가 어떻게 읽는가")을 **끝까지** 밀었다. 앞 회차는 `remark-gfm`
하나로 규칙을 맞췄는데, 답변 말풍선을 실제로 그리는 것은 그것 하나가 아니다 — `App.jsx`는
`REMARK_PLUGINS = [remark-gfm, remark-math, remarkLooseMath]`와 `REHYPE_PLUGINS`로 그린다.
그 차이를 그대로 재자 **서버가 채운 칸이 세 가지로 조용히 바뀌고 있었다**. 셋 다 오류를 한 줄도 남기지
않고 DB의 값과 화면의 값을 다르게 만드는, 이 저장소가 가장 나쁘게 보는 형태다.

### 결함 A — 대괄호가 든 칸이 대괄호를 잃고 수식으로 조판된다 (`chart.js escapeCell`)

앞 회차가 링크를 막으려고 `[`와 `]`를 함께 이스케이프하게 했는데, 그렇게 만들어진 `\[…\]`가 **그 자체로
별행 수식 표기**다. `remarkLooseMath`의 존재 이유가 "markdown이 백슬래시를 떼어 `[ ]`만 남기는 표기를
원문에서 되살리는 것"이라, 우리의 이스케이프가 곧 그 표기가 된다. 실측(프런트 실물 파이프라인):

| DB의 값 | 화면 |
|---|---|
| `[ERROR]` | (수식) `ERROR` — 대괄호 소실 |
| `[BATCH001] 실패` | (수식) `BATCH001` + ` 실패` |
| `상태[1]` | `상태` + (수식) `1` |
| `JSON: {"a":[1,2]}` | `JSON: {"a":` + (수식) `1,2` + `}` |

대괄호는 조회 결과에 흔하다(`[ERROR]`·`[WARN]`·배치 태그·배열 첨자) — 셋 중 가장 자주 나는 오답이다.
**수정:** 닫는 `]`만 막는다. 링크·이미지·각주는 닫는 쪽만 막아도 전부 성립하지 못하고(`[x\](y)`·
`![a\](b.png)`·`[a\][b\]`·`[^1\]` 전부 원문으로 남는 것을 실물 파서로 확인), `\[`가 만들어지지 않으므로
수식 표기도 생기지 않는다. 덤으로 값이 진짜 `\[y\]`인 경우까지 함께 고쳐졌다.

### 결함 B — `$`가 든 칸이 조판되어 달러 기호가 사라진다 (`chart.js escapeCell`)

`$`는 CommonMark의 구성요소가 아니라서 이스케이프 목록에 없었는데, 이 칸을 그리는 쪽은 `remark-math`
(`$$…$$`)와 `remarkLooseMath`(`$…$`)를 함께 건다. 실측: `$x$` → `x`, `수식 $a+b$ 참고` → `수식 a+b 참고`,
`$$100$$` → `100`. **수정:** `$`를 `*`·`` ` ``·`~`와 같은 '짝이 있을 때만' 목록에 넣었다. 홑 `$`는 손대지
않으므로 `$HOME`·`V$SESSION`·`$100 ~ $200`은 글자 하나 달라지지 않는다.

### 결함 C — 주소가 든 칸에서 이스케이프의 백슬래시가 화면과 링크 주소에 그대로 남는다 (`chart.js escapeCell`)

GFM은 주소 낱말을 통째로 자동 링크로 만들고 그 안에서는 아무것도 해석하지 않는다 — 즉 막을 것이 없는데,
막으면 백슬래시가 되돌려지지 않은 채 **화면 글자와 `href` 양쪽에** 들어간다. 실측:
`http://intra/a*b*c` → 화면·href 모두 `http://intra/a\*b\*c`(눌러도 404), `https://intra/~user/~tmp`,
`http://intra/r?a=1&amp;b=2`, ``https://host/p`q`r`` 도 같다. 조회 결과에 실린 주소는 눌러 보라고 있는 것이라
값이 바뀌는 것보다 이쪽이 먼저 눈에 띄지 않는다.

**수정:** '반드시 링크가 되는' 낱말 안에서는 막지 않는다. 판정은 파서(micromark)의 실제 동작을 좁게
흉내 낸다 — ① 글의 처음이거나 앞이 공백 ② `www.`·`http://`·`https://` ③ 도메인에 `_`가 없다
(`http://a_b.com/x`는 링크가 아니다 — 실측) ④ 앞에 짝 없는 `[`·`<`가 없다(그 뒤 자동 링크는 아예 생기지
않는다) ⑤ 낱말 안에 `<`·`]`가 없고 끝이 링크에서 떨어져 나가는 문장부호·엔터티가 아니다. 다섯이 모두
서야 '보호 구간 = 링크 구간'이 되어, 링크 안에 백슬래시가 들어가는 일도 링크 밖에 막지 않은 글자가
남는 일도 없다. 확신하지 못하는 낱말은 종전대로 막되 `$`만은 막지 않는다(이번에 새로 막기 시작한
글자라, 실제로는 링크였던 자리에 없던 백슬래시를 넣게 된다). 구간 안에서도 `\`·`|`는 그대로 막는다.

### 검증

수정 전/후를 같은 값으로 실물 파이프라인에 태워 대조하는 차분 퍼저를 만들어 돌렸다
(**FIXED / REGRESS / BOTH**로 가른다 — 회귀가 0이어야 한다):

| 축 | 무엇을 재는가 | 결과 |
|---|---|---|
| 차분 퍼저 (실무형 값) | 조회 결과에 나올 법한 조각 + 주소를 섞은 값 8시드 × 6,000건 | **FIXED 약 2,100/6,000 · REGRESS 0** |
| 차분 퍼저 (적대적 값) | 주소·문장부호를 공백 없이 이어 붙인 값 6시드 × 6,000건 | **FIXED 약 1,500/6,000 · REGRESS 0** |
| 답변 후처리 통짜 | 무작위 답변(문장 + `table`/`chart` 참조) × 무작위 행 3시드 × 400건 — 표 밖 문장 소실·칸 값 어긋남 | 0건 |
| 잘린 값 가드 | 새 규칙에서도 `clippedCopyDetector`가 칸에 보인 앞부분을 알아보는가 | 10/10 통과 |
| 성능 | 셀당 비용과 길이 선형성(주소가 든 긴 값 2k→16.8k) | ×2.08~2.32(선형), 100행 × 10열 표 한 장 약 1ms |
| E2E | 실 서버 + 가짜 SSE LLM + 실 관리 DB로 `run_query` → ```table 참조 채움 | 정상 |

정식 테스트 5개를 더했다(전부 수정 전 실패를 확인했고, 각 수정을 하나씩 되돌리는 변이 5종을 모두 잡는다):

| 테스트 | 무엇을 못 박는가 |
|---|---|
| `backend/test/chart.test.js` — 대괄호는 닫는 쪽만 막는다 | `\[`를 만들지 않는다는 것 자체가 계약이다(그것이 수식 표기다) |
| `backend/test/chart.test.js` — `$`는 짝이 있을 때만 막는다 | 홑 `$`(`$HOME`·`V$SESSION`)는 그대로, 짝은 막는다 + 되돌리는 쪽 길이 일치 |
| `backend/test/chart.test.js` — 값에 든 주소는 막지 않는다 | 보호하는 모양과 보호하지 않는 모양(도메인 `_`·앞의 `[`·꼬리 문장부호)을 함께 |
| `frontend/test/chart.test.js` — 말풍선이 실제로 쓰는 파이프라인에서도 원문 그대로 | `REMARK_PLUGINS`+`REHYPE_PLUGINS`로 렌더해 칸을 대조. 파서·플러그인을 바꾸면 여기서 먼저 깨진다 |
| `frontend/test/chart.test.js` — 칸에 든 주소는 화면 글자도 링크 주소도 원문 그대로 | `href`까지 본다 — 주소가 한 글자만 달라도 링크는 조용히 다른 곳을 가리킨다 |

기존 회귀 테스트(`차트 블록의 칸은 라벨과 표로 보기 양쪽에서 같은 글자다`)의 값 목록에 `$x$`·`[ERROR]`를
더했다 — 되돌리는 쪽(`frontend/src/chart.js splitRow`, `backend/src/agent.js unescapeCell`)에서 그 글자를
빼면 그 테스트가 잡는다(변이로 확인).

확인했으나 결함이 아닌 것:

- 화면 trace 패널(`TraceGrid`)은 셀을 markdown이 아니라 글자 그대로 그린다 — 이스케이프 규칙과 무관하다.
- GFM 표의 칸은 양끝 공백을 뗀다(` 값 ` → `값`). 표 문법 자체라 이스케이프로 지킬 수 없고, 종전과 같다.
- `MariaDB VEC_FromText`는 `JSON.stringify`가 만드는 지수 표기(`1e-7`)를 받는다(실 DB 확인 — 12.2.2).
- 모듈 수준 `g`/`y` 정규식의 `lastIndex` 누수 없음(`matchAll`은 사본을 쓰고, `y`는 쓰기 직전 `lastIndex`를 세운다).
- 백엔드/프런트가 나눠 든 상수(`MAX_CHAT_TURNS`·`MAX_CHAT_LEN`·`MAX_QUESTION_LEN`·차트 행 상한·`CONFIG_RE`)는 현재 전부 일치.

검증 환경은 macOS, Node 25.9.0, MariaDB 12.2.2, remark-gfm 4.x / remark-math 6.x / react-markdown 10.x
(프런트 의존성 실물). 실제 OpenRouter 응답과 Windows 배치 스크립트는 이번에도 검증 범위 밖이다.

| 최종 검증 | 통과 | 실패 |
|---|---:|---:|
| 단위·회귀·HTTP·프로세스 (`npm test`) | 493 | 0 |
| 실제 MariaDB 통합 | 12 | 0 |
| 실제 Oracle 통합 | 9 | 0 |
| 프런트엔드 회귀 (`npm test`) | 147 | 0 |

이 기록은 위 환경과 검토 범위의 결과이며 모든 입력과 운영 환경에서의 결함 부재를 보증하지는 않는다.
