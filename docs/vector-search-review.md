# 전체 검색 코드 검토 결과 — 2026-09-08

벡터 생성·동기화, 정확 쿼리명 조회, 처리방법 라우팅, 검색 제어, 근거 병합·확대,
프롬프트와 화면 표시까지 검토하고 재현된 결함을 수정했다. 아래 결과는 현재 코드와
자동 테스트의 검증 범위에 대한 기록이며 모든 데이터에서 ANN 검색의 완전한 재현율을 보장하는 뜻은 아니다.

## 추가 정밀 검토 — 기준 커밋 `702f083`

2026-09-08, 이전 검토 뒤 바뀐 거리 문턱 0.4와 최소 3건 보충을 포함해 현재 작업 트리를 다시 검토했다.
아래 범위표의 임베딩 생성·캐시·동기화, 세 소스의 후보 선정, 청크 병합·확대, 정확 이름 조회와
본문 라우팅, 검색 제어·프롬프트·로그·화면 표시를 현재 `context.md` 계약과 대조했다.

| 확정한 production 결함 | 수정 전 재현 | 수정 및 정식 회귀 검사 |
| --- | --- | --- |
| 정확 이름 조회 장애가 정상 0건으로 숨겨짐 | 새로 등록해 벡터가 아직 없는 쿼리의 이름 조회를 1회 실패시키면 검색을 성공 0건으로 기록하고, 같은 검색의 복구 시도를 반복 가드로 차단 | 벡터 후보도 없으면 `null`을 반환. `search.test.js`에서 실제 검색→에이전트의 실패 기록·프롬프트·진행 이벤트·재시도 성공까지 검사하며, 벡터 검색 성공 시에는 후보를 보존 |
| Mock·대체 답변에서 검색 실패가 사라짐 | 검색이 실패한 이력만 주면 Mock이 ‘등록된 지식에 없는 내용’이라고 답함. 일부 결과가 있는 대체 답변에도 실패 안내가 없었음 | `renderAnswer`가 검색 실패 안내를 포함. `llm.test.js`에서 정상/강제 Mock 답변, 정상 0건과의 구분, 확보한 지식·조회 결과 보존을 검사 |
| 주변 글자 때문에 Unicode 쿼리명 라우팅 누락 | 등록명 `Σ`, `ΟΣ`, `İΣ`가 `AΣ`, `ΟΣA`, `İΣA`에 그대로 있어도 본문 전체의 소문자 변환 때문에 누락 | 원문 구간을 찾아 `nameKey`로 재검증하는 공통 대조를 DB 라우팅과 Mock 계획에 적용하고 agent의 선행 소문자 변환을 제거. `search.test.js`, `constants.test.js` 및 실제 MariaDB 검색→Mock 실행 통합 테스트로 검사 |

세 결함 모두 production 수정 전에 정식 테스트의 실패를 확인했다. 재현 로그는 각각
`/tmp/search-audit-exact-before.log`, `/tmp/search-audit-failure-answer-before.log`,
`/tmp/search-audit-unicode-route-before.log`에 남겼으며, 수정 후 동일 검사들이 통과했다.
Unicode 대조는 추가로 원문 부분 문자열을 전수 대조하는 별도 실험 5,000건과 현재 Node 런타임의
Unicode 소문자 변환 1,488개도 확인했다. 서로 다른 악센트·이모지, 리터럴 정규식 기호,
`İ`의 여러 문자 변환과 거절한 후보에 겹친 다음 이름은 정식 회귀 테스트에 포함했다.

### ANN 테스트 실패의 원인 분리

기존 MariaDB 통합 테스트는 반복 UPDATE 뒤 ANN이 전수 검색과 같은 ID들을 반환한다고 가정했다.
실제로는 DB가 반환한 ANN 후보에서 이미 거리 0.39인 1번 행이 빠졌고, 같은 상태의
`IGNORE INDEX (embedding)` 조회에는 존재했다. 세 소스 모두에서 관측했으며, 진단 로그는
`/tmp/search-audit-ann-probe.log`다. 이는 [MariaDB가 명시한 근사 최근접 검색](https://mariadb.com/docs/server/reference/sql-structure/vectors/vector-overview)의
재현율 한계와 부합하며 애플리케이션의 거리 필터가 삭제한 결과는 아니었다.

검사 범위를 유지하면서 역할을 분리했다. 정상 ANN 경로의 인덱스·중복·정렬·반환 상한은 실제 DB에서
검사하고, 알려진 후보에 대한 거리 0.4 경계·최소 3건은 세 소스의 정식 단위 테스트로 검사한다.
정확한 ID와 개수도 최근접 고아 후보를 이용해 실제 SQL의 정확 보충 경로에서 그대로 검증한다.
임시 진단 프로그램은 제거했고 production의 ANN 방식과 탐색 깊이는 변경하지 않았다.

### 현재 작업 트리 검증

다음 명령이 모두 완료됐고, 총 821개 테스트가 통과했다. 실패·취소·건너뜀은 0개다.
뒤의 기존 검증 기록과 테스트 수는 이전 작업의 이력이다.

| 실행 위치·명령 | 결과 |
| --- | --- |
| `backend`: `npm test` | 558개 통과 |
| `backend`: `npm run test:integration` | 임시 MariaDB에서 48개 통과 |
| `frontend`: `npm run test:all` 중 `npm test` | 149개 통과 |
| 위 명령 중 `npm run test:ui` | 실제 Chrome에서 64개 통과. 검색 진행→검색 결과 패널 전환 포함 |
| 위 명령 중 `npm run test:production` | 현재 소스의 빌드와 Chrome production 화면 2개 통과 |

- `backend`에서 `SEARCH_LIMIT=1 node --test --test-name-pattern='세 소스의 실제 검색|지식 청크가 한 문서|Unicode 이름은' test/integration/mariadb.test.js`도 6개 통과했다.
  설정이 최소 3으로 보정된 상태의 세 소스 검색·문서 보충·Unicode 실행 검사이며, 위 전체 수에는 중복해서 더하지 않았다.
- `context.md`의 계약 설명을 갱신한 뒤 해당 문서 검증 8개와 `git diff --check`도 통과했다.
- 전체 실행 로그는 `/tmp/search-audit-backend-final.log`, `/tmp/search-audit-integration-final.log`,
  `/tmp/search-audit-frontend-final.log`에 있다. 실제 DB 검사는 운영 데이터와 분리한 임시 DB와
  결정적인 임베딩 응답 대역을 사용했다. 모델의 의미 검색 품질이나 운영 DB 데이터를 검사한 결과로 해석하지 않는다.
- 등록명 1,000개 대조와 상위 30개 상세 조회는 현재 실행에서 14.9ms였고, DB 왕복은 2회였다.
  모든 데이터 규모의 지연 시간을 보장하는 수치는 아니다.

요구사항별로 전체 경로 검토, 실패 재현 후 수정, 수정 후 정상 동작 검증, production 결함의
정식 회귀 테스트 추가를 모두 완료했다. 재현된 production 결함 3종과 테스트의 ANN 가정 오류를
처리했으며, 위 검증 범위에서 미해결 결함은 없다.

## 범위와 근거

| 경로 | 검토 내용 | 검증 위치 |
| --- | --- | --- |
| `embedding.js` | 모델·접두, 응답 순서/index, 차원, FP32 수치, 정규화, 오류·취소·타임아웃 | `embedding.test.js`, MariaDB 극단값 테스트 |
| `embed-sync.js` | 원문/벡터 해시 일치, 변경분 처리, 청크 트랜잭션, 배치 실패 복구, 삭제, 락, 종료 | `embed-sync.test.js`, MariaDB 동기화·CLI 테스트 |
| `search.js` | 세 소스 후보 선정, 현재 본문 검증, 후보 보충, 거리/개수 제한, 정확 쿼리명, 캐시 | `search.test.js`, MariaDB 실제 검색·실행 계획 테스트 |
| `chunk.js` | 분할 경계, UTF-16, 공백/반복 본문, 겹침 제거, 범위 계획, 길이 상한 | `chunk.test.js`, MariaDB v3→v4 전환 테스트 |
| `db.js`, 스키마·마이그레이션 | PK/벡터 인덱스, 청크 범위 읽기, 해시 표현, FK, 복구 SQL | MariaDB 스키마·시드·마이그레이션·복구 테스트 |
| `agent.js`, `context-items.js`, 프롬프트·서버 연결 | 검색 실패와 0건 구분, 근거 병합·확대 중 버전 변경, 표시 상한, 시작·종료 배선 | `agent.test.js`, `context-flow.test.js`, `prompt.test.js`, `server.test.js` |
| `db.js`, `agent.js` 쿼리 라우팅 | 이름 정확 조회, 본문 등장 순서, 한글 조사·짧은 이름·대소문자·리터럴 기호, 직접 검색과 병합·장애 복구 | `search.test.js`, `agent.test.js`, MariaDB 실제 라우팅 테스트 |
| `llm.js`, `result.js`, 프런트엔드 `stream.js`, `trace.js`, `App.jsx` | 검색 결정 정규화, 반복·부분 실패·재시도, 결과 직렬화, 스트림 수신·검색 진행 및 결과 표시 | 백엔드 관련 단위 테스트, 프런트엔드 단위 테스트, Chrome 검색 화면 테스트 |

## 재현 후 수정한 결함

| 결함 | 재현 | 최종 처리 |
| --- | --- | --- |
| 수정된 본문과 이전 벡터 연결 | 재임베딩 실패 시 기존 벡터와 새 본문이 함께 검색됨 | 동기화와 검색이 같은 모델·본문 해시를 사용 |
| 무효 후보가 정상 결과를 가림 | 오래된 상위 60개 뒤의 정상 61번째 지식이 0건 처리됨 | LEFT JOIN으로 무효 후보를 감지하고 필요할 때 한 번 정확 검색; 고아 벡터도 동일 처리 |
| 잘못된 벡터가 저장·캐시에 성공으로 남음 | 영벡터 동기화 성공 후 재시도 없음; 잘못된 차원/영벡터를 받은 질의는 서버 복구 후에도 0건 | 저장·캐시 전 1024차원·FP32 유효성 검사, 실패 캐시 제거, 다음 요청/동기화에서 복구 |
| 극소 벡터의 거리 계산 오류 | 성분 `1e-23` 벡터가 기준 벡터와의 거리를 잘못 계산 | 유효한 응답을 FP32 단위 벡터로 정규화 |
| 설정 변경 후 이전 질의 캐시 사용 | 같은 검색어의 접두/서버 변경 후 임베딩 호출 없음 | 서버·모델·실제 전송 입력을 캐시 키에 포함 |
| 긴 공백 뒤 본문 손실/중복 | 800자 본문 + 긴 개행 + 반복 본문을 병합하면 원문과 달라짐 | 청크 내부 공백과 공백 청크 보존, 분할 규칙 v4로 자동 전환 |
| 보충 검색 실패가 정상 후보까지 버림 | 새 보충 경로에서 DB 오류 발생 시 이미 검증한 후보도 null 처리됨 | 정상 후보는 보존; 정상 후보가 없으면 검색 불가로 보고 |
| 일부 누락된 청크가 복구되지 않음 | 4개 중 1개를 삭제한 뒤 동기화해도 3개만 유지됨 | 기존 GROUP BY에서 판본·개수·연속 번호·총수를 검증하여 재생성; 정상 seq 보존, 누락 벡터 1건만 재계산 |
| 정상 Unicode 쿼리명이 정확 검색에서 제외됨 | DB에 등록 가능한 이모지 60자의 이름을 임베딩 없이 검색하면 null 반환 | 등록명과 소문자 변형을 모두 포함하는 UTF-16 200자 상한으로 판단 |
| 검색한 Unicode 쿼리명이 프롬프트에서 잘림 | 같은 이름이 상세·간략 후보 목록에서 잘려 실행 식별자를 잃음 | 쿼리 이름은 최대 UTF-16 200자로 표시하고 실행 이력 및 전체 예산 계산에도 동일 상한 적용 |
| DB collation 충돌로 다른 쿼리를 정확 조회·실행 | `📈조회`가 등록된 `📊조회`로 조회되고 미등록 이름의 실행 요청이 실제 실행 함수까지 도달; `resume`도 `résumé`로 조회됨 | 인덱스 조회 후 공통 `nameKey`로 후보 재검증; 정상 대소문자 조회 유지 |
| 처리방법이 지목하지 않은 Unicode 쿼리 선택 | 본문의 `📈조회`가 `LOCATE`에서 등록된 `📊조회`에 적중 | 공통 JavaScript `nameKey`로 본문 대조; 다른 이모지를 구분하고 등장 순서 유지 |
| Unicode 대소문자 규칙 차이로 정상 쿼리 누락 | `𐐀조회`, `ΟΣ`는 본문 라우팅에서 누락되고 `İ조회`의 소문자 변형은 정확 이름 조회에서 누락됨 | 이름 비교를 JavaScript `nameKey`로 통일; 정확 조회는 인덱스가 놓친 경우 이름·번호만 보충 조회 |
| ASCII로 소문자화되는 Unicode 등록명 누락 | 등록명 `K조회`를 Unicode 소문자형 `k조회`로 검색하면 UNIQUE 인덱스와 보충 조건이 모두 놓쳐 0건 반환 | `k`가 포함된 미적중 이름도 Unicode 보충 대상으로 분류하고 실제 MariaDB 회귀 검사 추가 |
| 소문자 변형의 문자 수 증가로 정확 검색 제외 | `İ` 60자 등록명의 소문자형이 120자가 되어 임베딩 없는 검색에서 null 반환 | 소문자화로 늘어나는 길이까지 포함하는 UTF-16 200자 상한 적용 |
| 일반 미등록 검색어마다 등록명 전체 조회 | 정확 이름이 없는 평범한 자연어 검색도 Unicode 보충 경로에 들어가 등록 건수에 비례해 이름을 읽음 | 비ASCII 대소문자 변환·결합 문자가 있는 이름만 Unicode 보충; 일반 검색은 UNIQUE 인덱스 조회 1회 뒤 벡터 검색으로 진행 |
| 종료 신호 뒤 저장 재시도가 계속되고 마지막 배치의 종료 상태 유실 | 배치 실패 시 종료해도 쓰기 4회(필요 1회), 첫 행 재시도 후 종료해도 4회(필요 2회); 마지막 배치의 skipped가 false | 새 저장·행별 재시도 경계에서 종료 확인, 미처리 행은 실패 수에서 제외, 마지막 배치도 STOPPED 전파 |
| 복구 전 극소 벡터가 정상 검색 결과로 노출 | 올바른 해시를 가진 극소 벡터 60개 중 19개가 관련 결과로 반환되고 정상 후보와 섞임 | ANN 후보의 유클리드 노름을 검증하고 불량 후보가 있으면 안전 범위의 벡터만 대상으로 보충 검색 |
| 긴 문서 청크 저장 중 종료해도 나머지 쓰기와 커밋을 계속함 | 첫 청크 뒤 종료 요청에도 나머지 9개 청크를 쓰고 트랜잭션 커밋 | 청크별 쓰기·꼬리 삭제 경계에서 종료 확인, 트랜잭션 롤백 후 다음 실행에서 전체 문서 복구 |
| 고아 벡터 정리 중 종료해도 후속 스캔·삭제를 계속하고 종료 상태를 잃음 | 원본 스캔·벡터 스캔·첫 삭제 배치 뒤 종료 시 각각 4회·3회·2회의 추가 DB 작업 발생 | 스캔·삭제 배치 경계에서 중단하고 마지막 소스에서도 STOPPED 전파; 다음 실행이 남은 고아만 정리 |
| 청크 재생성 스캔이 종료 신호를 무시함 | 스캔 전·원문 스캔 도중 종료 시 각각 2회·1회의 추가 전체 스캔 발생 | 재생성 진입과 원문 스캔 직후 종료 확인; 정상 종료를 청크 실패로 세지 않음 |
| 삭제된 변경 행의 빈 본문 조회가 종료 신호를 잃음 | 마지막 소스의 변경 행 1건이 삭제되면 종료 요청에도 정상 완료로 기록; 1,001건이면 다음 본문 조회 1회 추가 | 본문 조회 직후 행 유무와 무관하게 종료 확인; 추가 읽기를 중단하고 STOPPED 전파 |

각 결함은 정식 테스트에서 실패를 확인한 뒤 수정했다. 기존의 청크별 trim 테스트는 내부 공백을
보존하는 새 계약으로 변경했으며, 문서 전체의 바깥 공백만 정리한다. 정상 테스트용 임베딩도 실제
스키마와 같은 1024차원으로 변경하여 차원 검증을 우회하지 않는다.

## 이전 검증 — `85be619`

2026-09-08(KST), `85be619`의 현재 production 코드를 검색 계약과 다시 대조했다.
기존 수정은 `21fe546`과 `85be619`에 반영되어 있으며, 이번 재검토에서 추가로 확정된
production 결함은 없다. 세 소스의 정렬·반환 상한·거리 문턱·정상 0건 검사를 정식 MariaDB
통합 테스트에 보강했다. 다음 다섯 테스트 실행은 모두 현재 작업 트리에서 완료됐다.

- `cd backend && npm test`: 534개 통과, 실패/건너뜀 0개.
- `cd backend && npm run test:integration`: 45개 통과, 실패/건너뜀 0개.
- `cd frontend && npm test`: 149개 통과, 실패/건너뜀 0개.
- `cd frontend && npm run test:ui`: 실제 Chrome 전체 UI 테스트 64개 통과, 실패/건너뜀 0개.
  검색 진행→결과 패널 전환, 후속 질문·홈 복귀와 결과 표시 검증을 포함한다.
- `cd frontend && npm run test:production`: 현재 소스 빌드와 Chrome production 화면 검사 2개 통과,
  실패/건너뜀 0개. 총 794개 테스트가 통과했다.
- 추가한 통합 검사는 지식·처리방법·쿼리 각 소스에 상한보다 많은 후보를 넣고 PK 역순으로
  가까워지는 벡터를 사용한다. 상위 후보만 관련도 순서대로 반환되고, 별도 검색에서는 거리
  0.54인 자료만 남고 0.56인 자료는 제외됨을 확인했다. 모두 무관하면 `null`이 아닌 `[]`다.
- `SEARCH_LIMIT=1 node --test --test-name-pattern='세 소스의 실제 검색' test/integration/mariadb.test.js`
  (backend에서 실행): 허용된 최소 상한에서도 같은 검사 4개 통과, 실패/건너뜀 0개.
  위 794개와 중복되는 검사를 설정만 바꾼 실행이므로 전체 테스트 수에 더하지 않았다.

아래는 앞선 재현·수정 과정의 근거다. 정식 테스트에 포함된 검사는 위 전체 실행에서도 통과했다.
종료 처리의 수정 전 실패 로그(`/tmp/vector-before-reproduction.log`)도 다시 확인했다.

- 부분 누락·Unicode 이름 결함은 수정 전 정식 테스트의 실패를 확인했다. 수정 후 누락된
  청크만 재임베딩되고, 정상 쿼리명이 임베딩 없이 검색되어 프롬프트에도 온전히 실렸다.
- 실제 DB의 처리방법 검색에서 등록 순서와 다른 본문 순서대로 쿼리 후보가 표시되는지 확인했다.
  한글 조사와 한 글자 이름을 인식하고, `_`를 와일드카드로 오인하여 다른 이름을 고르지 않았다.
- 문자열 비교 충돌을 실제 DB에서 재현했다. 수정 전 다른 쿼리의 실행 함수 호출이 1회였고,
  수정 후 0회이며 미등록 오류가 기록됐다. 정확 검색·본문 라우팅 모두 다른 Unicode 이름을
  제외하고 정상 이름과 대소문자 변형은 그대로 반환하는 정식 회귀 검사를 추가했다.
- 정식 테스트에 시드가 고정된 1,000개 문서의 분할·병합 원문 보존 검사를 포함했다.
  별도 검토에서도 공백·반복 문구·이모지를 섞은 5,000개 문서의 원문 불일치가 0건이었다.
- 1,000행 테스트 데이터의 실제 EXPLAIN에서 정상 검색은 벡터 `embedding` 인덱스와 원본 PK
  `eq_ref` 조회를 사용했다. 정상 검색은 1회 조회, 무효 후보 발생 시 총 2회 조회였다.
- 기존 영벡터·극소 벡터 복구 SQL의 멱등성, 정상 벡터 보존, 누락분만 재임베딩을 검증했다.
- 캐시에서 퇴출된 이전 요청의 늦은 실패가 같은 검색어의 새 진행 중 요청을 지우지 않는지
  정식 회귀 테스트를 추가했다. 새 요청을 중복 호출하지 않고 정상 결과를 공유했다.
- 실제 DB의 등록명 1,000건에서 이름 메타데이터는 `query_name` 커버링 인덱스를 사용했고,
  본문 상위 30건의 상세만 읽었다. 총 DB 조회는 2회, 최종 테스트의 대조·상세 조회 실측은 4.1ms였다.
  데이터 규모나 환경 전반의 지연 시간 보장으로 해석하지 않는다.
- 이름 메타데이터 확인과 상세 조회 사이 등록명이 바뀌면 다른 SQL을 반환하지 않는지 검사했다.
  정확 이름 보충과 처리방법 라우팅 두 경로 모두 현재 이름을 재검증했다.
- 일반 자연어의 정확 이름 조회가 실패할 때 등록명 메타데이터 전체를 읽지 않고 UNIQUE 인덱스 조회
  1회 뒤 벡터 검색으로 이어지는지 정식 회귀 테스트로 고정했다.
- 저장 중 종료 요청을 배치 실패·행별 재시도 성공/실패·배치 성공·임베딩 행별 복구의 5가지 조합으로
  검증했다. 새 쓰기는 중단되고 실제 성공/실패만 집계되며, 다음 실행에서 미처리 행만 복구한 뒤
  다시 실행하면 재임베딩이 0건이었다. 진행 중인 DB 문장을 즉시 취소한다는 보장은 포함하지 않는다.
- 수동 복구 SQL을 실행하지 않은 상태에서도 영벡터와 극소 벡터가 검색 결과에 나오거나 정상 후보를
  가리지 않는지 실제 DB에서 검증했다. 극소 벡터는 수정 전 19건이 잘못 반환됐고 수정 후 0건이었다.
  정상 검색의 EXPLAIN은 변경 후에도 VECTOR `embedding` 인덱스와 원본 PK `eq_ref`를 사용했다.
- 긴 지식의 청크 트랜잭션에서 첫 쓰기 직후 종료 신호를 주는 정식 테스트를 추가했다. 수정 후
  추가 쓰기·커밋은 0건, 롤백은 1건이며 다음 실행이 전체 청크를 복구하고 세 번째 실행은 0건이었다.
- 추가 검토에서 고아 벡터 2,001건을 대상으로 원본 스캔·벡터 스캔·첫 삭제 배치의 종료를 각각
  재현했다. 수정 전 후속 DB 작업은 4회·3회·2회, 수정 후 모두 0회다. 삭제 건수는 실제 완료한
  0건·0건·1,000건이며, 다음 실행이 남은 전부를 정리하고 세 번째 실행의 삭제는 0건이다.
  청크 스캔 전·도중 종료의 후속 스캔도 2회·1회에서 모두 0회로 바뀌었다.
  다섯 시나리오는 `backend/test/embed-sync.test.js`의 정식 테스트이며 모두 수정 전 실패를 확인했다.
- 변경 행이 스캔과 본문 읽기 사이에 삭제되는 경우도 정식 테스트로 추가했다. 변경 행 1건에서는
  종료 상태가 `false`로 유실되고 1,001건에서는 종료 뒤 본문 읽기 1회가 더 발생하는 것을 수정 전
  확인했다. 수정 후 두 조건 모두 추가 읽기 0회, `skipped: stopped`, 임베딩·실패 0건이다.
  `85be619`의 종료 처리 수정은 직전 `21fe546` 코드의 임시 복사본에서도 재현했다. 실제 작업
  트리를 되돌리지 않았으며, 7개 종료 시나리오가 모두 수정 전 실패하고 수정 후 통과했다.
- 수정 당시 백엔드·MariaDB 통합 테스트, 프런트엔드 단위·Chrome 검색 화면 검사와
  `cd frontend && npm run build`가 통과했다(번들 크기 경고만 발생). 현재 재검증은 위 다섯 실행 결과다.
- `git diff --check` 통과.

실행 환경은 Node.js 25.9.0, MariaDB 12.2.2였다. 통합 테스트는 기존 DB와 분리된 임시 Unix 소켓
DB를 사용하고, 임베딩 응답은 정상·실패·잘못된 값·순서 변경을 제어하는 대역으로 검증했다.
소켓을 사용하는 전체 테스트는 샌드박스의 `listen EPERM`을 확인한 뒤 승인된 환경에서 재실행했다.
위 통과 수는 해당 재실행 결과이며, 권한 오류를 코드 결함이나 성공으로 처리하지 않았다.
현재 재검증에서 제한 환경의 세 실행이 모두 종료 코드 1로 끝난 것을 확인한 뒤 허용 환경에서
다시 실행했다.

현재 재검증의 원시 로그는 `/tmp/search-audit-backend-allowed.log`,
`/tmp/search-audit-integration-final.log`, `/tmp/search-audit-frontend-allowed.log`,
`/tmp/search-audit-ui.log`, `/tmp/search-audit-production.log`, `/tmp/search-audit-limit-one.log`에 있다.
임시 로그와 별개로 재현·정상 동작 검사는 위 정식 테스트에 남는다.

이전 실제 연결 점검에서는 설정된 `bge-m3` 서버의 2개 응답이 모두 1024차원이고 정규화 후
길이가 약 1임을 확인했다. 실제 모델과 임시 MariaDB를 연결한 별도 전체 경로 검증도 통과했다:
지식·처리방법·쿼리 각 1건을 임베딩·저장하고 동일 입력으로 검색하여 각각 1건, 거리 0으로 찾았다.
변경 없는 두 번째 동기화에서는 재임베딩이 0건이었다.

그때 기존 관리 DB는 직접 연결 검사에서도 `ECONNREFUSED`로 연결이 거부되었다.
따라서 기존 데이터에 대한 검색은 확인하지 못했다. 이는 임시 DB에서 검증한 코드 동작과 구분한다.

## 적용 및 성능 고려

코드 반영 후 첫 동기화에서 기존 v3 청크는 자동으로 재분할된다. 기존 자리는 seq를 유지하고,
텍스트 해시가 바뀐 청크만 다시 임베딩한다. 스키마 변경은 없다.

이전 버전에서 불량 벡터가 저장됐던 설치는 `backend/sql/repair-invalid-vectors.sql`을 관리 DB에
한 번 실행한 뒤 `npm run embed`로 누락분을 다시 계산한다. 실행 예시는 README의 벡터 검색 절에 있다.
복구 SQL은 정상 단위 벡터와 원본을 보존한다. 운영 DB의 복구·서버 배포는 이번 코드 검증에 포함하지 않았다.

정확 검색 보충은 무효 후보가 실제로 있을 때만 수행한다. 이 경로는 전체 행에 비례한 비용이 들 수
있으며 `MARIADB_TIMEOUT_MS`의 제한을 받는다. 정상 데이터에서의 인덱스 경로와 장애 시 정합성을
각각 테스트했고, 모든 데이터 규모에서 일정한 지연 시간이나 완전한 ANN 재현율을 보장하지 않는다.
정상 ANN 경로는 상위 후보 최대 60개의 벡터 노름을 추가 계산한다. 이는 기존 불량 벡터가 올바른
해시를 가진 채 남아 있어도 검색을 오염시키지 않기 위한 비용이며 DB 왕복은 늘리지 않는다.
불량 후보가 실제로 발견될 때만 기존의 정확 보충 조회가 한 번 추가된다.
청크 누락 검증은 이미 수행하던 집계에 값만 추가하므로 DB 왕복이나 원문 전송을 늘리지 않는다.
정확 쿼리명이 기존 UNIQUE 인덱스에서 확인되면 1회 조회다. 일반 한글·숫자·이모지 이름과 대부분의
ASCII 이름이 확인되지 않아도 1회 조회 뒤 벡터 검색으로 진행한다. DB와 JavaScript의 Unicode 변환
결과가 달라질 수 있는 비ASCII 대소문자 문자·결합 문자, `K`의 소문자형과 구분할 수 없는 `k`가
포함된 미적중 이름만 이름·번호 목록을 1회 보충 조회하며, 일치하면 그 PK의 상세를 1회 더 읽는다.
이 Unicode 보충 적중은 최대 3회 조회다.
처리방법 라우팅은 등록명 메타데이터 조회와 상위 30건 상세 조회의 최대 2회다. 기존 1회보다
왕복은 늘지만 긴 SQL·설명을 등록 건수만큼 읽지 않으며, 정확 조회·실행과 이름 비교가 일치한다.
메타데이터 스캔 비용은 등록 건수에 비례한다. 잘못된 이름 결과를 피하기 위해 요청 사이 캐시는 두지 않는다.

## 이전 검토의 요구사항별 완료 확인

1. 전체 검색 코드 검토: 위 범위표의 생성·동기화·벡터/이름 검색·라우팅·에이전트 제어·근거 표시 경로와
   `context.md`의 검색 계약을 대조했다. 인덱스 경로, 실패 시 복구, 반복 제한, 결과 표시를 각각 검증했다.
2. 실제 결함 확정 후 수정: 위 결함마다 수정 전 실패를 재현했다. DB의 collation·거리 계산·인덱스
   동작은 대역의 추측으로 확정하지 않고 실제 MariaDB에서 검증했다.
3. 수정 후 정상 동작: 백엔드·DB 통합·프런트엔드 단위·Chrome 전체 UI·production 빌드 검사
   794개에 실패·건너뜀이 없다. 기존 운영 DB 데이터 확인은
   연결 거부로 수행하지 못했으며, 이를 코드 테스트 성공에 포함하지 않았다.
4. production 결함의 정식 테스트: 재현 검사는 `backend/test`의 일반 또는 통합 테스트에 포함했다.
   일회성 실험만으로 수정 완료를 판정하지 않았다. 정상 경로와 장애 복구도 함께 검사한다.
