# 컨텍스트 관리 규칙

핵심은 **요청 중 확보한 자료는 보관하고, 이번 LLM 호출에 필요한 부분만 표시하는 것**이다.
검색 결과를 표시 예산 때문에 삭제하지 않는다. 동일한 ID로 다시 읽을 수 있다.
이 파일은 현재 계약이며, 변경 전 설계와 장애 대응 경위는 [이전 기록](docs/context-history.md)에 보존한다.
숫자와 주요 관계식은 `backend/test/context-doc.test.js`가 코드와 대조한다.

## 1. 프롬프트

| 순서 | 블록 | 내용 |
|---|---|---|
| 1 | 관련 지식 | 검색한 구간, 안정적인 ID, 보관 목록 |
| 2 | Q&A 처리 방법 | 처리 절차, 안정적인 ID, 보관 목록 |
| 3 | 실행 가능한 쿼리 목록 | 이름·용도·바인드·입력 설명·대상 DB |
| 4 | 실행 이력 (검색·쿼리) | 검색 상태, 실행 결과의 표시 범위, 추가 읽기 실패 안내 |
| 5 | 최근 대화 | 클라이언트가 보낸 최근 발화 |
| 6 | 사용자 질문 (현재) | 이번 질문 |
| 7 | 지시 | 현재 시각과 남은 행동 횟수 |

시스템 프롬프트에는 요청마다 달라지는 값을 넣지 않는다. 동일한 앞부분은 서버가 prefix caching을
지원할 때 재사용할 수 있다. 검색·확대·결과 재표시로 자료가 바뀌면 그 부분의 재사용도 달라진다.
`context.md` 자체를 LLM에게 보내지는 않는다. 실제 조립은 `llm-openai.js buildPrompt`가 한다.

자료 상태는 세 가지로 구분한다.

- 미검색: 섹션이 없고, 아직 검색하지 않았다는 안내가 있다.
- 검색 성공·0건: 섹션에 `(없음)`이 있다.
- 검색 실패: 실행 이력에 `검색 불가`가 있다. 등록 자료가 없다는 뜻이 아니다.

찾지 않은 대상에 `0건`이나 `검색 불가`를 기록하지 않는다. 처리방법을 통해 쿼리 정의를 읽은 것과
`query` 검색 성공도 별도로 센다.

## 2. 자료 보관과 표시

### 검색

```json
{"action":"search","text":"고객 주문 상태","targets":["knowledge","qa_method","query"]}
```

- 사내 자료가 필요할 때 모델이 검색한다. 후속 질문의 대상은 최근 대화에서 복원한다.
- 지식·처리방법은 벡터 검색이다. 소스별 벡터 테이블을 사용하고 검색어 임베딩은 공유한다.
- 쿼리는 **검색어 전체와 query_name의 정확 일치**를 먼저 찾는다. 적중하면 임베딩 없이 반환한다.
  일치하지 않으면 벡터 검색한다. 문장 안에서 쿼리명을 추측해 추출하지 않는다.
- 세 대상의 검색은 병렬이다. 정확 이름 조회 실패가 성공한 벡터 검색 결과를 없애지 않는다.
  이름 조회가 실패하고 벡터 후보도 없으면 정상 0건 대신 검색 불가로 남겨 재시도를 허용한다.
- 처리방법 본문이 지목한 쿼리도 함께 읽는다. 정확 일치 쿼리 → 처리방법이 지목한 쿼리 → 직접 검색
  순으로 합치고, `MAX_PROMPT_QUERIES` 30개까지 반환한다. 직접 검색의 상위 `DETAIL_TOP` 5개는 상세 표시 대상이다.
  이름은 본문 원문에서 찾아 해당 구간의 `nameKey`로 확인한다. 주변 글자가 Unicode 대소문자
  변환을 바꾸더라도 실제 적힌 이름을 보존하고, 다른 악센트·기호를 같은 이름으로 고르지 않는다.
- 벡터 거리 문턱은 `MAX_DIST` 0.4다. 문턱 안의 결과가 `MIN_SEARCH_RESULTS` 3개보다 적으면
  문턱 밖에서도 가까운 순서로 보충한다. 유효한 자료 자체가 부족하면 있는 만큼 반환한다.
  지식은 청크 병합 뒤의 구간 수를 기준으로 하며, 상위 청크가 한 문서에 몰린 경우에는
  문서별 최근접 청크를 추가 조회한다. 오래된 판본·고아 행·무효 벡터는 보충 대상에서도 제외한다.
  정확한 쿼리명 조회는 기존처럼 해당 쿼리만 반환한다. 가까운 거리만으로 정답이라고 판단하지 않는다.
- 같은 검색어·대상의 반복은 실행하지 않는다. 다만 요청한 대상이 **전부 '검색 불가'**였던 시도는 반복으로 세지
  않는다 — 아무것도 찾아보지 못했으므로 한 번 더 시도할 수 있고, 두 번째까지 성립하지 않으면 끊는다.
- Mock과 LLM 장애 시 대체 답변도 검색 실패를 알리고, 확보한 지식·조회 결과는 함께 표시한다.
  검색 실패만으로 등록된 자료가 없다고 단정하지 않는다.

### 지식 구간

| 설정 | 값 |
|---|---|
| `CHUNK_TARGET_LEN` | 900자 |
| `CHUNK_MAX_LEN` = `MAX_PROMPT_ITEM_LEN` | 1,000자 |
| `CHUNK_OVERLAP` | 150자 |
| `CHUNK_GAP_FILL` | 2청크 |
| `MAX_DOC_LEN` | 문서별 표시 본문 합계 10,000자 |
| `MAX_EMBED_TEXT_LEN` | 임베딩 원문 4,000자 |

지식은 원문 대신 청크를 검색한다. 가까운 적중 사이의 빈 청크만 보충하고, 떨어진 적중은 각각의
구간으로 보존한다. 대표가 있는 구간 하나만 남기지 않는다. 제목에 `(3~7/22)`처럼 표시한 범위를 적는다.
청크 상한·문서 상한은 표시 형태의 들여쓰기까지 포함해 검증한다.

요청 안에서는 다음 세 규칙만 적용한다(`context-items.js`).

1. 기존 ID와 근거를 보존한다. 겹치는 구간 하나와 합쳐도 상한 안이면 기존 ID로 합친다.
   합칠 때 검색이 확정한 '더 받을 것이 없다'(full) 판정은 지우지 않는다 — 한쪽이 full이면 합친 구간도 full이다.
2. 합칠 수 없으면 이미 보관한 청크를 빼고 새 구간을 추가한다. 같은 문서라도 떨어진 절은 별도 ID다.
3. 표시할 때 문서별 본문 총량을 제한하고 동일한 청크를 두 번 싣지 않는다. 표시에서 빠진 원본은 보관한다.

같은 문서를 다시 검색했다고 기존 구간을 교체하지 않는다. 펼친 구간과 새로 검색한 다른 절도 함께 남는다.
원본은 요청이 끝나면 해제한다. 다음 사용자 질문까지 자료 캐시를 유지하지 않는다.

### ID, 확대, 복구, 숨김

```json
{"action":"expand","ids":["k12"],"drop":["m3"]}
```

- `k`는 지식 구간, `m`은 처리방법이다. ID는 청구 횟수나 본문 길이와 무관하게 유지한다.
- `확대 가능`은 본문을 더 읽을 수 있다는 별도 표시다. 번호가 있다는 이유만으로 확대하지 않는다.
- `expand`는 앞뒤 청크를 넓히거나, 숨겨진 항목을 복구하거나, 보관된 항목을 앞으로 가져온다.
  숨김 복구에는 DB 읽기가 필요 없다. 이미 표시된 짧은 첫 항목의 반복 청구는 진도가 아니다.
- 확대한 항목은 표시 우선순위를 유지한다. 같은 문서의 표시 총량은 여전히 `MAX_DOC_LEN` 안이다.
- `drop`은 search·expand에 선택적으로 붙인다. 본문을 숨길 뿐 원본·ID를 지우지 않는다.
  숨긴 구간은 재검색만으로 복구하지 않고 expand로 명시적으로 복구한다.
  실제로 숨긴 항목이 있으면 새 자료가 없어도 그 스텝은 진도로 센다 — 자료가 달라졌기 때문이다.
  (가드에 걸려 실행하지 않은 검색은 예외다. 이미 한 검색을 다시 낸 결정이므로 그대로 센다.)
- 생략·숨김 항목은 보관 ID 목록으로 표시한다. 목록도 예산에 포함하며, 아주 길면 ID 단위로 생략한다.
  ID 중간을 잘라 다른 식별자로 만들지 않는다.
- 확대·복구·우선 표시를 합해 `MAX_EXPANDS` 2건이다. 남은 수가 0이어도 ID는 유지하고 지시가 제한을 알린다.
- 보관된 항목을 앞으로 가져오는 일에는 DB 읽기가 필요 없다 — 확대가 한 글자도 늘리지 못해도(상한 포화·읽기 실패)
  그 항목은 앞으로 와서 본문이 실린다. 이미 맨 앞이라 옮길 자리가 없을 때만 늘지 않은 이유를 알린다.
  읽기 실패는 기존 본문과 확대 판정을 그대로 보존한다.
- 확대가 같은 문서의 다른 보관 구간을 통째로 삼키면 그 구간의 본문은 이미 앞 항목의 줄에 실려 있다.
  그 번호를 청구해도 앞으로 가져오지 않고 이미 실려 있다고 알린다 — 새로 보이는 글자 없이 문서 상한만
  나눠 쓰면 지금 보이던 본문이 그만큼 줄기 때문이다. 문서 바깥쪽으로 넓힐 것이 남아 있으면 그 청구는 그대로 성립한다.
  숨김 복구도 같은 판정을 받는다: 숨김 자체는 걷지만 앞으로 가져오지는 않는다. 확대와 함께 버린 번호를
  뒤늦게 되살리는 흐름이 정확히 이 경우다.

## 3. 쿼리 실행과 결과 읽기

쿼리는 **짧게 표시할 때도 입력 설명을 포함한다.** 이름·용도·바인드명·입력 의미/형식과, 선택지가 여러
개인 대상 DB를 표시한다. 입력 설명이 없으면 미등록이라고 명시하고 값을 추측하지 않도록 안내한다.
입력 설명 상한은 `MAX_PROMPT_ITEM_LEN` 1,000자다. 잘린 설명은 절단 표시를 남긴다. 등록 설명은 이 안에
필수 의미·형식·허용값을 적는다. 자유 텍스트 설명이므로 타입·날짜 형식을 자동 검증하는 스키마는 아니다.

상세 항목에는 긴 용도 설명·출력 설명이 추가된다. SQL 원문은 모델이 선택한 쿼리에만 표시한다.
선택된 쿼리의 상세 표시는 다른 후보보다 먼저 예산을 배정한다 — 실행한 쿼리는 펼친 지식처럼 목록 앞에 고정되어
뒤 검색이 후보를 얹어도 밀리지 않는다. 여유가 있어도 모든 후보에 SQL을 싣지 않는다.
기존의 읽기 전용 SQL 검사, 바인드 검증, 대상 DB 검증, 중복 실행 가드는 그대로 적용한다.
독립 조회는 `run_queries`로 일괄 실행하고, 앞 결과에 의존하는 조회는 순차 실행한다.

```json
{"action":"read_result","step":2,"cols":["NEXT_JOB_ID"],"offset":20,"limit":5}
```

- 이미 실행한 조회의 `fullRows`를 읽는다. DB를 다시 실행하지 않고 실행 횟수도 쓰지 않는다.
- `step`은 실행 이력의 1부터 시작하는 번호다. 검색 줄도 번호를 차지한다.
- `offset`은 0부터 시작하는 행 위치다. `limit`은 양의 정수이며 최대 `MAX_RESULT_ROWS` 20행이다.
- `cols`는 정확한 컬럼명 목록이다. 생략하면 전체 컬럼이다. 잘못된 이름·없는 실행·범위 밖 행은 안내로 반환한다.
- 해당 실행의 **프롬프트 표시 범위만 교체**한다. 원본·로그의 초기 미리보기·실행 번호·표와 차트의 참조는 바꾸지 않는다.
- 읽은 부분에도 결과 JSON 상한을 적용하고 실제 표시한 행 위치·건수를 밝힌다. 넓은 결과는 필요한 컬럼만 고른다.
- 컬럼 생략 안내는 실제 데이터 컬럼과 구별한다. `…`가 실제 컬럼명이어도 값과 타입을 유지하고,
  시스템 안내는 충돌하지 않는 이름으로 표시한다. 추가 읽기에도 이 구분을 유지하며,
  최종 표·차트에서는 시스템 안내만 제외하고 실제 컬럼은 표시한다.
- 드라이버가 이미 잘라 저장한 셀은 복원하지 못한다. 추가 읽기는 보관된 행과 컬럼을 다시 보는 기능이다.
- 실행 이력의 긴 파라미터에도 절단 표시를 남긴다. 표시된 조각에서 생략 표시를 떼고 바인드로 재사용해도
  실행 가드가 거부한다. 파라미터 JSON은 값 중간에서 자르지 않고, 예산에 들어가지 않는 바인드는 건수로 알린다.
- 성공·실패 시도를 합해 `MAX_RESULT_READS` 2회다. 반복·잘못된 읽기도 가드와 시간 예산을 적용한다.
  실패 안내는 실행 이력 몫 안에 최대 200자로 표시하며, 새 실행 번호를 만들지 않는다.

날짜 결과의 후속 바인드는 등록 SQL의 타입·포맷과 맞아야 한다. `DATE`는 초까지,
`TIMESTAMP` 계열은 드라이버의 JavaScript Date가 보존한 밀리초까지 표시한다.
세션의 `NLS_TIMESTAMP_FORMAT`은 `YYYY-MM-DD HH24:MI:SS.FF3`,
`NLS_TIMESTAMP_TZ_FORMAT`은 `YYYY-MM-DD HH24:MI:SS.FF3 TZH:TZM`이다.
시간대 있는 결과는 오프셋을 포함한다. `TIMESTAMP WITH LOCAL TIME ZONE`에 그 문자열을
바인드하는 등록 SQL은 `TO_TIMESTAMP_TZ(:at, 'YYYY-MM-DD HH24:MI:SS.FF3 TZH:TZM')`처럼
오프셋을 명시적으로 해석한다. 밀리초보다 정밀한 값이 필요하면 조회 SQL에서
`TO_CHAR(..., 'YYYY-MM-DD HH24:MI:SS.FF9')`로 반환하고, 후속 조회도 같은 포맷으로 변환한다.
이 경우 원래 정밀도의 문자열이 보관되므로 추가 읽기도 그 값을 그대로 제공한다.

## 4. 프롬프트 예산

전체 상한 + 섹션별 기본 몫과 천장을 유지한다. 배분 순서는 아래 표 순서이고, 표시 순서와는 별개다.
앞 섹션이 실제로 쓰지 않은 예산은 뒤로 넘어간다. 뒤가 남긴 예산을 앞으로 돌리는 재배분은 하지 않는다.

| 설정 | 기본 | 천장 |
|---|---:|---:|
| `MAX_PROMPT_TOTAL_LEN` | 80,000자 | 자료 네 섹션과 고정 틀의 합계 |
| `PROMPT_FRAME_RESERVE` | 500자 | 제목·지시·빈 줄 (실측 최대 425자) |
| `PROMPT_FLOORS.history` | 29,000자 | 줄 수와 개별 결과·파라미터·식별자 상한으로 제한 |
| `PROMPT_FLOORS.queries` | 15,000자 | `PROMPT_CEILINGS.queries` 20,000자 |
| `PROMPT_FLOORS.qaMethods` | 10,000자 | `PROMPT_CEILINGS.qaMethods` 20,000자 |
| `PROMPT_FLOORS.knowledge` | 25,000자 | `PROMPT_CEILINGS.knowledge` 40,000자 |
| 기본 합계 | 79,500자 | 여유 500자 |

| 개별 상한 | 값 |
|---|---:|
| `MAX_PROMPT_ITEM_LEN` | 1,000자 |
| `MAX_DOC_LEN` | 한 문서의 표시 본문 합계 10,000자 |
| `MAX_EXPANDED_ITEM_LEN` | 펼친 처리방법 본문 4,500자 |
| `MAX_PROMPT_NAME_LEN` | 제목 100자 |
| `MAX_PROMPT_QUERY_NAME_LEN` | 쿼리명 200 UTF-16 코드 유닛 (`VARCHAR(100)` 보존) |
| `MAX_TARGET_DB_NAME_LEN` | 대상 DB 이름 200 UTF-16 코드 유닛 (`VARCHAR(100)` 보존) |
| `MAX_PROMPT_SHORT_DESC_LEN` | 짧은 쿼리 용도 120자 |
| `MAX_PROMPT_SQL_LEN` | 선택한 쿼리 SQL 800자 |
| `MAX_PROMPT_STEP_LEN` | 실행 결과 JSON 3,000자 |
| `MAX_PROMPT_PARAMS_LEN` | 실행 이력 params 500자 |
| `MAX_PROMPT_SEARCH_LEN` | 실행 이력 검색어 200자 |
| `MAX_RESULT_ROWS` | 프롬프트 결과·로그 미리보기 20행 |
| `MAX_ROWS` | 조회 원본 보관·화면 최대 1,000행 |
| `MAX_CELL_LEN` | 결과 셀 200자 |
| `MAX_RESULT_COLS` | 결과 컬럼 30개 |

예산 밖의 몫도 별도 상한을 가진다.

- 최근 대화: `MAX_CHAT_TURNS` 6턴 × 1,500자 = 9,000자 (`MAX_CHAT_LEN`).
- 질문: `MAX_QUESTION_LEN` 2,000자.
- 시스템 프롬프트: `MAX_SYSTEM_PROMPT_LEN` 6,000자. 모듈 로드 시 검증한다.

문자 수 합계는 80,000 + 9,000 + 2,000 + 6,000 = **97,000자**다.
기존 산문·SQL·결과 JSON 표본의 환산은 약 54~65k 토큰이지만, 모든 문자열이나 모델에 대한 토큰 상한은 아니다.
출력 상한 `MAX_COMPLETION_TOKENS` 16,384와 함께 실제 모델의 컨텍스트 크기에 맞춰 검증한다.
이번 변경에서 전체 예산이나 검색 후보 수를 낮추지는 않는다. 정확도를 확인하면서 표시량부터 조정한다.

## 5. 행동 제한과 불변식

| 설정 | 값 | 의미 |
|---|---:|---|
| `MAX_SEARCHES` | 3 | 검색 횟수, 환경변수로 낮출 수 있음 |
| `SEARCH_LIMIT` | 20 | 검색당 소스별 최대 후보 구간/행 수, 환경변수로 3~20 설정 가능 |
| `MIN_SEARCH_RESULTS` | 3 | 벡터 검색의 최소 후보 구간/행 수, 유효한 자료 부족 시 있는 만큼 반환 |
| `MAX_STEPS` | 5 | 조회 시도 수 |
| `MAX_BATCH_QUERIES` | 4 | 결정 하나의 독립 조회 수 |
| `MAX_EXPANDS` | 2 | 자료 확대·복구·우선 표시 수 |
| `MAX_RESULT_READS` | 2 | 보관된 조회 결과의 추가 읽기 시도 수 |
| `MAX_DROPS` | 40 | 결정 하나에서 숨길 수 있는 항목 수 |
| `MAX_HISTORY_ROWS` | 9 | `MAX_STEPS` + `MAX_SEARCHES` + 안내 1 |
| `MAX_GUARD_HITS` | 2 | 연속으로 진도가 없으면 강제 답변 |
| `MAX_LOOP_MS` | 180,000ms | 요청 시작부터 재는 루프 시간 예산 |

루프 결정 상한은 `MAX_STEPS` + `MAX_SEARCHES` + `MAX_EXPANDS` + `MAX_RESULT_READS` = 12회다.
진행 중 I/O와 마지막 강제 답변 호출은 기존 타임아웃을 따른다. `MAX_LOOP_MS`가 HTTP 응답 전체의 하드 제한은 아니다.

검증할 불변식:

- 기본 몫 합계 + 고정 틀 ≤ 전체 상한. 개별 자료·ID 목록·오류 안내도 모두 해당 몫에 포함한다.
- 이력은 쿼리 줄 `MAX_STEPS`개 + 나머지 자리를 가장 긴 비쿼리 줄로 계산해 어떤 조합도 들어야 한다.
- 펼침 총량: 지식 2 × 10,000자, 처리방법 2 × 4,500자가 각각 기본 몫 안에 들어야 한다.
- 같은 청크는 표시에서 중복되지 않고, 새 검색이나 표시 제한이 보관된 다른 절을 지우지 않는다.
- 복구해도 ID가 바뀌지 않으며 숨긴 본문이 폴백 답변에 섞이지 않는다.
- 결과 추가 읽기는 새 DB 실행·이력 번호를 만들지 않고 기존 표·차트 참조를 유지한다.
- 임베딩 실패와 0건을 구분하고, 정확 쿼리명 적중은 임베딩 없이도 동작한다.

## 6. 사용자 출력과 측정

표·차트 참조는 최종 답변이 정해진 뒤 서버가 원본 결과로 채운다.
답변 천장은 `MAX_ANSWER_LEN` 70,000 + `MAX_TABLE_INJECT_LEN` 30,000 + `MAX_CHART_INJECT_LEN` 30,000 = **130,000자**다.
드라이버 오류 원문과 내부 복구 지침은 사용자 출력에 그대로 싣지 않는다. 내부 자료 ID도 최종 답변에 노출하지 않는다.

측정은 `chat_log`와 요청별 `timing`, `search` 요약을 사용한다.

- 요청 전체의 LLM 호출 수, 누적 prompt/completion 토큰, 응답 시간.
- 검색 대상별 성공·실패·적중 수, 확대·숨김 수, `resultReads` 추가 읽기 시도 수.
- `trace.search.top`의 거리와 문서 정보는 진단 자료다. 거리만으로 정답 여부를 판정하지 않는다.
- 평가 질문에는 필요한 근거 구간, 쿼리명, 대상 DB, 파라미터를 함께 지정한다.
  검색 성공률과 실제 프롬프트의 근거 보존율, 쿼리 선택/실행 정확도, 최종 답변 정확도를 따로 비교한다.

실제 임베딩 모델·업무 데이터로 평가하기 전에는 후보 수나 본문 상한의 최적값을 단정하지 않는다.
