# 컨텍스트 관리 규칙

이 문서는 **무엇이 LLM 프롬프트에 들어가고, 어떻게 들어오고, 얼마나 실리고, 어떻게 나가는가**의 규칙을 한자리에 모은다.
상수 하나를 고칠 때 함께 봐야 할 것이 무엇인지도 여기 적는다.

이 규칙들이 조용히 깨진다는 것이 이 문서가 있는 이유다. 예산이 어긋나면 등록이 늘어난 어느 날부터
"모든 질문이 LLM 호출 실패"가 되고, 자료가 밀려 잘리면 오류 없이 답변만 부실해진다. 값은 대부분
`backend/src/constants.js`에 있고, 그 밖에 있는 것은 아래 표에 파일명을 적었다. 숫자는 전부 코드에서 뽑은
현재 값이고 `test/context-doc.test.js`가 문서와 코드를 대조한다 — 이 문서가 낡는 것도 위 두 실패와 같은
부류이기 때문이다.

---

## 1. 프롬프트의 모양

한 번의 LLM 호출에 실리는 것은 이 순서다. **자료를 먼저, 과제를 마지막에** 둔다.

| # | 블록 | 내용 | 언제 실리나 |
|---|---|---|---|
| 1 | 관련 지식 | 검색된 `knowledge` 항목 | `knowledge`를 검색해 성립한 뒤 |
| 2 | Q&A 처리 방법 | 검색된 `qa_method` 항목 | `qa_method`를 검색해 성립한 뒤 |
| 3 | 실행 가능한 쿼리 목록 | `query_registry` 항목 | `query`를 검색했거나 처리방법이 지목했을 때 |
| 4 | 실행 이력 (검색·쿼리) | 이 요청이 지금까지 한 일 | 항상 |
| 5 | 최근 대화 | 클라이언트가 보낸 최근 턴 | 항상 |
| 6 | 사용자 질문 (현재) | 이번 질문 | 항상 |
| 7 | 지시 | 현재 시각 + 다음 행동 요구 | 항상 |

순서가 이런 이유는 둘이다.

- 모델은 마지막에 읽은 것을 가장 강하게 붙든다. 결정할 대상인 질문이 자료 수천 자 앞에 묻히면 안 된다.
  특히 후속 질문("그럼 김철수는?")은 짧아서 앞에 두면 그대로 파묻힌다.
- 한 요청 안에서 스텝마다 바뀌는 것은 실행 이력과 지시뿐이다. 요청마다 바뀌는 것(질문·대화·시각)까지
  뒤로 몰면 앞부분이 스텝 사이에 같은 토큰열로 남아 vLLM prefix caching이 그만큼을 재사용한다.
  같은 이유로 **시스템 프롬프트에는 요청마다 달라지는 값을 하나도 싣지 않는다**.

### 세 가지 상태를 구분한다

자료 섹션은 "없음"과 "못 찾아봤음"을 절대 섞지 않는다. 섞으면 모델이 "등록된 자료가 없다"고 단정한다.

| 상태 | 프롬프트에 어떻게 보이나 |
|---|---|
| 아직 찾아보지 않았다 | 섹션 자체가 없다. 지시 블록에 "아직 검색한 자료가 없다" 한 줄 |
| 찾아봤는데 없다 | `## 관련 지식 (0건)` + `(없음)` |
| 찾아봤지만 검색이 성립하지 않았다 | 섹션 없음. 실행 이력 줄에 `지식 검색 불가` |

임베딩 서버가 없으면 검색은 0건이 아니라 **검색 불가**다 (`backend/src/search.js`가 `null`을 돌려준다).

같은 이유로 **찾아보지 않은 대상은 이력 줄에도 적지 않는다.** `qa_method`만 찾은 검색은 처리방법이 지목한
쿼리를 경로A(2-6)로 함께 싣는데, 지목된 것이 없을 때 그 줄에 `쿼리 0건`을 적으면 모델은 `query`를 이미
찾아본 것으로 읽고 건너뛴다(`agent.js absorb`). 지목된 것이 있을 때만 `쿼리 N건`이 붙는다.
`검색 불가`도 같은 규칙이다 — 경로A의 쿼리 목록 로드가 실패해도 `query`를 찾지 않은 검색의 줄에는
`쿼리 검색 불가`를 적지 않는다(그 실패는 `trace.search.queriesFailed`가 남긴다, 8-). 적던 동안 모델은
찾지도 않은 대상을 '지금은 확인할 수 없다'고 답했고, chat_log에는 `searchFailed`가 서서 임베딩 장애로 집계됐다.

---

## 2. 자료가 들어오는 길

컨텍스트는 비어서 시작한다. **모델이 요청해야 채워진다.** 인사 한 줄에 지식·처리방법·쿼리를 미리 실어
보내던 구조를 뒤집은 것이다 — 그때는 첫 호출의 prefill이 질문과 무관하게 최대치였다.

### 2-1. 검색 (`search`)

```json
{"action":"search","text":"핵심 낱말","targets":["knowledge","qa_method","query"],"drop":["k7"]}
```

- 검색어는 **모델이 쓴다**. 후속 질문이면 대화에서 대상을 복원해 적는다.
- 대상은 셋 중 필요한 것만 고른다. 모르면 셋 다 (`normalizeSearchTargets`의 기본값).
- 벡터 검색 단일 경로다. 세 대상은 병렬로 돌고 임베딩은 검색어당 한 번만 계산된다.
  임베딩 저장소는 **소스마다 테이블이 따로**다(`vec_knowledge_chunk`·`vec_qa_method`·`vec_query_registry`).
  한 테이블에 담고 `src`로 거르던 구조에서는 ANN이 상위 K건을 고른 *뒤* 걸러지므로, 큰 소스가 그 K건을
  차지해 작은 소스의 검색이 조용히 0~2건으로 주저앉았다.
- **지식은 원문이 아니라 청크를 검색한다**(2-5). 나머지 둘은 행 단위 그대로다.
- **처리방법을 찾으면 그 본문이 지목한 쿼리가 함께 실린다**(경로A). 절차만 있고 쿼리 정의가 없으면
  실행할 수 없어 왕복만 하나 더 늘기 때문이다.

### 2-2. 병합 규칙

이번 검색이 찾은 것은 — 새 항목이든 이미 있던 항목이든 — 관련도 순으로 **목록 맨 앞에** 온다
(`agent.js mergeFront`). 예외는 하나 — **펼친 항목은 넘어서지 않는다.** 목록 앞머리의 펼침 구간 뒤에
끼운다(2-3). 있던 항목을 제자리에 두면 이번 검색의 1위가 지난 검색의 꼬리에 남아 잘린다.

항목의 정체 키는 **청크 항목이면 `doc_seq`, 아니면 `seq`** 다. 청크 항목의 `seq`는 '가장 가까운 청크'의
것이라(2-5) 두 번째 검색에서 다른 청크가 대표가 되면 값이 달라진다 — `seq`로만 거르면 같은 문서가 두
항목으로 들어와 지식 몫을 두 번 먹는다. 같은 문서가 다시 오면 항목을 새로 만들지 않고 **그 항목의 구간을
이번 검색의 것으로 바꾼다.** `seq`는 그대로다 — 모델이 이미 지목한 번호가 요청 도중 다른 것을 가리키면
안 된다는 것이 **식별자 설계의 근거**다(2-3). 이번 구간이 이미 실린 구간 안에 들면 넓은 쪽을 둔다.
먼저 온 구간을 무조건 지키던 동안에는 뒤 검색이 같은 문서의 다른 절(3번 청크 → 15~17번)을 찾아도 그 절이
통째로 버려지고, 모델은 그것이 존재한다는 사실조차 볼 수 없었다. 펼친 항목만 구간도 지킨다 — 모델이 청구해
넓힌 것이다. 다른 절이 필요하면 버리고(2-4) 다시 찾으면 그 절이 실린다.

돌려주는 값은 **진도**다 — 새로 넣었거나 내용이 달라진(구간이 바뀐, 쿼리의 자세한 표시가 오른) 항목 수.
순서만 바뀐 것은 세지 않는다. 이 수가 0인 검색이 연속 `MAX_GUARD_HITS`번이면 강제 답변으로 간다.

앞에 붙이는 이유: 프롬프트 예산은 "뒤쪽일수록 덜 관련됐다"는 전제로 꼬리부터 버린다. 방금 요청한 검색이
가장 관련 높다는 전제다. **이 전제가 검색 2회째부터 깨진다** — 그것이 `drop`이 존재하는 이유다(2-4).

`drop` 없이 두 번째 검색을 내면 무슨 일이 일어나는지는 숫자로 정해져 있다. 지식 몫은 기본 25,000자이고, 앞 섹션이
남긴 여유가 있어야 천장 40,000자까지 간다(3-1). **지식 항목의 크기를 정하는 것은 항목 상한(1,000자)이 아니라 문서당
상한(`MAX_DOC_LEN` 10,000자)이다** — 항목 하나가 청크 하나가 아니라 '문서의 한 구간'이기 때문이다(2-5). 그래서
실리는 건수는 등록된 글의 길이가 정한다: 1,000자 이하의 글이면 기본 몫에도 후보 20건이 다 실리지만, 긴 문서를 병합한
10,000자짜리면 기본 몫에 **2건**, 천장에서도 4건이다. 검색 한 번이 `SEARCH_LIMIT`(20)건을 그 앞에 쌓으므로 긴 문서가
후보에 오르는 요청에서는 **1차 검색 결과가 거의 남지 않는다**(상한 4,500자·몫 10,200자이던 때의 실측은 0건).
되돌릴 방법도 좁다 — 같은 검색어는
중복 검색 가드가 막고, 밀려난 항목은 다른 검색어의 검색에 다시 걸릴 때만 앞으로 돌아온다. 쿼리 목록에는
바로 이 자리에 복구 경로가 있지만(짧은 줄 → 이름 지목 → `resolveQuery`가 다시 앞에 넣는다, 2-6)
**지식·처리방법에는 이름으로 부르는 길이 없다.** 그래서 시스템 프롬프트가 "두 번째 검색부터는 `drop`을 함께 적고, 앞선 검색에서
아직 쓸 것이 있으면 이번 검색의 `targets`를 좁히라"고 명시한다.

### 2-3. 본문 청구 (`expand`)

```json
{"action":"expand","ids":["k12"],"drop":["k7"]}
```

청크 구조에서 이 행동의 뜻은 **"본문 더 보기"가 아니라 "구간 넓히기"** 다. 청크는 잘리지 않으므로
(2-5) 더 받을 것은 그 문서의 앞뒤 청크뿐이다. 항목이 곧 구간이라 **같은 번호를 다시 청구하면 더
넓어진다** — 별도 항목으로 넣지 않는 이유는 그러면 한 문서가 여러 항목으로 흩어져 2-2의 문서 단위
중복 제거와 어긋나기 때문이다. 청크가 아닌 항목(처리방법)은 종전대로 한 번만 펼쳐진다.

- `k`는 지식, `m`은 처리방법의 `seq`다. 제목을 쓰지 않는 이유: `title`은 VARCHAR(200)인데 프롬프트에는
  100자로 잘려 실리므로 긴 제목은 모델이 온전한 형태를 본 적이 없다. 목록 위치도 쓸 수 없다 — 검색 결과가
  앞에 붙을 때마다 바뀐다. `seq`만 요청 내내 고정이다.
- 쿼리는 제외한다. `query_name`이 짧은 식별자로 설계돼 온전히 실리므로, 한 대상에 표기를 둘 만들 이유가 없다.
- **번호는 더 받을 것이 남은 항목에만 붙는다.** 청크 항목은 세 조건이 함께 서야 한다 — 범위 밖에 청크가
  남아 있고, 문서당 글자 상한(`MAX_DOC_LEN`)에 아직 닿지 않았고, **이웃 조각이 그 상한에 들어간다**
  (`chunk.js canGrow`, 세 번째는 `buildItems`가 세우는 `full` 표시). 청크가 아닌 항목은 종전대로
  '잘렸고 아직 안 펼친 것'이다. 청구할 수 있는 자리에만 보이게 해서 모델이 받을 수 없는 것을 청구하느라
  스텝을 버리지 않게 한다. 번호가 사라지는 것이 "더 받을 것이 없다"는 표시다.
  판정을 '잘렸는가'로 두면 안 된다 — 청크는 잘리지 않으므로 긴 문서에도 번호가 한 번도 붙지 않는다.
  앞의 두 조건만 보아도 안 된다 — 900자 청크면 다섯 조각(3,900자)에서 여섯째(4,650자)가 막히는데, 그 항목은
  범위도 남고 상한에도 안 닿아 번호가 남았고, 그 번호로 청구한 `expand`는 한 글자도 늘리지 못했다(실측).
  그래서 검색은 계획된 범위의 앞뒤 한 조각을 **싣지 않고 읽기만 해서**(`search.js`) 검색 시점에 이 판정을
  확정하고, 창을 넓혀 읽은 뒤에야 알게 되는 경우(`agent.js applyExpand`)는 늘지 않아도 표시를 받아 적어
  다음 프롬프트에서 번호가 사라지게 한다 — 그때의 안내는 '번호가 붙은 항목만'이 아니라 '더 넓힐 수 없다'다.
  읽기 자체가 실패하면(관리 DB 타임아웃·접속 실패) 항목도 번호도 그대로 남으므로 안내는 '읽어 오지 못했다 —
  다시 청구하지 말라'다. 그 자리에 '번호가 붙은 항목만'을 적던 동안 모델은 번호가 붙은 항목을 방금 청구하고도
  그 말을 받아 같은 번호를 다시 청구했고, 두 번째 실패에서 강제 답변으로 넘어갔다(실측).
- 펼친 항목은 **목록 맨 앞으로 옮기고, 이후 검색도 그 앞을 넘지 못한다.** 예산이 뒤에서부터 버리므로 그
  자리라야 살아남는다. 옮기기만 하고 지켜주지 않던 동안에는 청구가 통째로 헛돌았다 — 뒤이은 검색 한 번이
  후보 20건을 그 앞에 쌓으면 넓힌 구간이 몫 밖으로 밀려나는데, 펼친 항목에는 번호가 붙지 않으므로
  모델은 그것이 사라진 것을 볼 수도 다시 청구할 수도 없고 청구 기회만 하나 잃었다(실측).
- `MAX_EXPANDS`는 **넓힌 횟수**다 — 항목 하나를 한 번 넓힌 것이 1이고, 항목 수도 결정 수도 아니다. 같은 항목을
  두 번 넓히는 것이 정상 경로가 되면서, 항목 수로 세면 그것이 한 개로 세어져 상한이 걸리지 않는다. 결정 수로도
  세지 않는다 — 한 결정의 `ids` 둘이 다 넓혀지면 둘로 센다(`agent.js applyExpand`). 그래야 펼침 총량
  (`MAX_EXPANDS` × `MAX_DOC_LEN`, 5-)이 결정 하나에 실린 항목 수와 무관하게 지켜진다.
- 성공하면 실행 이력에 남기지 않는다. 본문이 길어지는 것으로 프롬프트에 그대로 드러난다
  (쿼리 상세 경로와 같은 방식이다). 펼칠 것이 없거나, 상한에 닿았거나, 본문을 읽어 오지 못했을 때만 안내 한 줄이 남는다.

### 2-4. 버리기 (`drop`)

**자료를 늘리는 행동만 자료를 줄일 수 있다.** `search`와 `expand`에만 얹힌다.

- 검색이 한 번뿐인 요청에서는 목록이 관련도 순이라 예산의 꼬리 버리기가 이미 옳은 정리다. `drop`이 할 일이 없다.
- 두 번째 검색부터 새 결과가 앞에 붙어 관련도 순서가 섞인다. 두 검색의 관련도를 예산은 견줄 수 없고
  **모델만 그 판단을 할 수 있다.** 그 판단이 필요한 순간이 정확히 두 번째 검색을 내는 그 순간이다.
- `expand`는 한 항목이 최대 `MAX_DOC_LEN`(10,000자)을 가져가므로 자리를 내줄 것을 함께 말하게 한다.
- **버린 항목은 목록에서 지우지 않고 표시만 세운다.** 병합이 문서 단위(청크 항목은 `doc_seq`, 나머지는 `seq`)로
  중복을 거르므로, 남겨 두어야 같은 항목이 재검색으로 되살아나지 않는다.
- 버리기는 검색보다 **먼저** 적용한다. 새 결과가 병합되기 전에 표시가 서 있어야 방금 버린 것이 그 검색으로
  되살아나지 않는다.
- 효력은 **그 요청 안에서만**이다. 다음 질문까지 남기면 한 번의 오판이 계속 따라다니고 보이지 않는다.
- 그 요청 안에서는 **같은 내용을 되돌릴 수 없다.** 병합이 문서 단위로 중복을 거르므로 재검색으로도
  돌아오지 않고, 모델이 보는 것은 "버림 3건"이라는 수뿐이라 무엇을 버렸는지조차 알 수 없다. 버리기가
  자료를 늘리는 행동에만 얹히므로, 오판을 알아차려도 검색이나 청구를 한 번 더 태워야 그 자리를 다시 쓸 수 있다.
- **되살아나는 것은 다른 구간뿐이다.** 버린 청크 항목에 뒤 검색이 **겹치지 않는** 다른 구간을 걸면 그 구간으로
  되살아난다(같은 번호). 모델이 버린 것은 그 구간이지 문서가 아니고, 되살리지 않으면 관련 있는 절이 이 요청
  안에서 영영 실리지 않는다. 겹치는 구간은 같은 내용이라 버린 채로 둔다. 펼친 항목을 버리고 다시 찾는 것이
  '청구한 구간 대신 다른 절을 받는' 유일한 길이기도 하다(2-2).
- 섹션 제목이 `## 관련 지식 (5건, 버림 3건)`으로 버린 수를 따로 밝힌다. 건수만 줄이면 모델이 자기가 버린
  것을 길이 제한으로 잘린 것으로 읽는다.
- **폴백 답변도 버린 항목을 붙이지 않는다**(`agent.js fallbackAnswer`). LLM이 끝내 결정을 내지 못하면 손에 든
  지식으로 답을 조립하는데, 모델이 무관하다고 판정한 본문이 그 판정을 무시한 채 '관련 지식'으로 나가면 안 된다.

### 2-5. 긴 지식이 실리는 길 (청크)

지식만 **원문이 아니라 청크를 검색한다.** 임베딩은 행 하나에 벡터 하나이고 원문은
`MAX_EMBED_TEXT_LEN`(4,000자)에서 잘리므로, 2만 자짜리 지식을 등록하면 검색은 앞 20%만 보고
판단하고 뒤 16,000자는 어떤 경로로도 모델에 닿지 않았다 — 프롬프트 상한도 같은 앞부분만 보여준다.
오류가 한 줄도 남지 않는 오답이라 이 저장소가 가장 나쁘게 보는 형태다.

나눠 두면 실리는 것이 **'문서의 앞 4,000자'가 아니라 '질문에 맞는 구간'** 이 된다. 그것이 이 변경의 전부다.

**처리방법은 나누지 않는다.** 짧고, 경로A(2-6)가 본문 전체를 훑어 쿼리 이름을 찾으므로 쪼개면 이름이
청크 경계에 걸려 라우팅이 조용히 끊긴다.

| | 값 | 왜 |
|---|---|---|
| 청크 목표 | 900자 (`CHUNK_TARGET_LEN`) | 경계를 문단·문장에서 찾을 여유 |
| 청크 상한 | 1,000자 (`CHUNK_MAX_LEN` = `MAX_PROMPT_ITEM_LEN`) | **검색된 청크가 프롬프트에서 다시 잘리지 않는다** |
| 겹침 | 150자 (`CHUNK_OVERLAP`) | 경계에 걸친 문장이 어느 한쪽에는 온전히 남는다. **이어 붙일 때는 뗀다**(아래) |
| 구멍 메우기 | 2청크 (`CHUNK_GAP_FILL`) | 3·5번이 걸리면 4번도 싣는다 |
| 문서당 상한 | 10,000자 (`MAX_DOC_LEN`) | 검색 판정이 틀렸을 때의 보험. **프롬프트에 실리는 형태로 잰다**(아래) |

**절단 위치는 서로게이트 쌍을 가르지 않는다.** 경계 탐색이 찾은 자리는 개행·문장 부호라 안전하지만 강제 절단과
겹침 시작은 임의의 코드유닛 위치다 — 이모지 한가운데면 앞 청크가 상위 서로게이트로 끝나고 다음 청크가 하위로
시작해, 임베딩 서버가 그 행을 매 주기 거부하고 프롬프트의 LLM 호출이 인코딩 단계에서 실패한다(실측).
`chunk.js alignCut`이 한 칸 앞으로 옮긴다 — `clipText`가 절단면에서 하는 일과 같다. 겹침 시작이 그렇게 한 칸
물린 이음매는 실제 겹침이 `CHUNK_OVERLAP` + 1 코드유닛이다 — 이어 붙이는 쪽(`cutSeam`)은 뒤 청크가 쌍으로 시작할 때
그 한 칸을 함께 본다. 보지 않던 동안 그 이음매는 어떤 길이에서도 맞지 않아 한 글자도 떼지 못했고, 151자가 개행까지
붙어 두 번 실렸다(퍼징으로 잡았다).

**경계 탐색은 CR(`\r`)도 개행으로 본다.** 등록 본문은 Windows 편집기에서 붙여 넣은 CRLF일 수 있고, 프롬프트 형태
(`indentLines`)와 답변 표는 이미 그것을 개행으로 다룬다. 분할기만 LF만 보던 동안 CRLF 문서의 빈 줄과 줄 끝 문장 끝이
한 번도 경계가 되지 못해, 한 줄에 한 문장씩 끝나는 절차 안내문이 이음매마다 강제 절단으로 떨어져 낱말 한가운데에서
갈렸다(실측: 4개 이음매 전부). 그래서 판(`CHUNK_SPLIT_VERSION`)을 올렸다 — LF 문서는 같은 자리에서 나뉘므로 다시
나눠도 내용이 같아 재임베딩이 없다(운영 데이터 145청크 실측: 벡터 해시 불일치 0건).

**등록은 안 바뀐다.** `knowledge`에 원문 한 행 그대로 넣으면 `embed-sync`가 청크를 만든다 —
기동 시 / `EMBED_SYNC_INTERVAL` 주기 / `npm run embed`. 문서 MD5가 바뀐 것만 재분할하고, 그다음
청크 MD5가 바뀐 것만 재임베딩한다. 원문은 손대지 않으므로 분할 규칙을 바꾸면 **다음 동기화가 알아서 다시 나눈다** —
문서 해시가 분할 규칙(크기·겹침, 그리고 절단 위치 규칙의 판 `CHUNK_SPLIT_VERSION`)을 포함하므로 상수 한 줄을
고치면 전 문서가 stale이 된다. 절단 위치를 고르는 규칙을 고쳤으면 그 판을 올려야 한다 — 안 올리면 기존 설치의
청크는 원문을 고치기 전까지 옛 규칙대로 남는다. 되돌릴 수 없는 변환이 아니다.

**글자 수는 프롬프트에 실리는 형태로 잰다.** 항목 본문은 목록 한 줄('- ')의 연속 줄로 두 칸 들여 실리므로
(`constants.js indentLines`, `llm-openai.js itemLine`) 줄이 많은 본문은 원문보다 길어진다. 병합(`chunk.js
buildItems`)과 번호 판정(`canGrow`)이 원문 길이로 재던 동안, 4,499자짜리 병합 항목이 프롬프트에서 355자를
잃고 `…(생략)`을 달고 나갔다(실측) — '청크는 프롬프트에서 다시 잘리지 않는다'가 이 자에서만 깨져 있었다.

**병합.** 검색은 청크 단위로 걸리지만 프롬프트에는 문서 단위로 실린다. 같은 문서의 적중을 잇고
사이 구멍을 메워 하나의 연속 구간으로 만들고, 제목에 위치를 박는다 — `[운영 가이드 (3~7/22)]`.
모델이 '긴 문서의 일부'임을 알아야 뒷부분을 못 본 채 단정하지 않는다. 청크가 잘리지 않게 되면서
사라진 `…(생략)` 표시가 하던 일을 이 표기가 대신한다.
**같은 표기가 폴백 답변에도 붙는다**(`llm.js renderAnswer`, 7-). 구간은 낱말 한가운데에서 시작할 수
있어 — 46조각 문서의 9~14번 구간이 `s, continuous batching…`으로 시작했다(실측) — 표기 없이 나가면
사용자가 그 조각을 등록된 지식의 전부로 읽는다. LLM이 죽었을 때 사용자가 읽는 유일한 본문이다.

**이음매의 겹침은 뗀다**(`chunk.js cutSeam`). 겹침은 '각 청크가 홀로 검색되게' 하려고 둔 것이지
다시 이어 붙일 때까지 두 번 실으라는 뜻이 아니다. 그대로 이으면 이음매마다 150자가 되풀이된다 —
운영 데이터의 5청크 구간에서 3,787자 중 594자(15.7%)가 같은 문장이었고, 모델은 문장을 읽고 나서
그 문장의 꼬리를 낱말 한가운데부터 다시 읽었다(실측). 손해는 셋이다: 문서당 상한의 6분의 1이 중복에
쓰이고, 그만큼 상한에 일찍 닿아 `full`이 서므로 본문을 덜 싣고 청구 경로까지 먼저 닫히며(그 구간은
`full=true`였다), 폴백 답변(`agent.js fallbackAnswer`)에서는 그 되풀이가 사용자에게 그대로 나간다.
**저장된 청크는 건드리지 않는다** — 분할 규칙도 문서 해시도 그대로라 재분할·재임베딩이 없다.
이웃한 번호끼리만 떼고, 겹침이라고 부를 만큼 길게 맞을 때만 뗀다 — 끝 한두 글자가 우연히 같다고
떼면 본문이 소리 없이 사라진다.

**뗀 이음매에는 구분자를 넣지 않는다.** 겹침을 뗀 뒤 남는 앞머리는 원문에서 앞 청크의 끝에 곧바로
이어지는 자리이고, 앞 청크가 저장될 때 잘려 나간 꼬리 공백(문단 사이의 빈 줄까지)이 그 앞머리에
그대로 들어 있다 — 그래서 붙이기만 하면 원문이 정확히 복원된다. 앞 공백을 떼고 개행 하나로
대신하던 동안 이음매마다 원문이 세 가지로 어긋났다(운영 데이터 135개 이음매 실측): 문단 경계
30자리가 빈 줄을 잃어 제목이 앞 문단에 붙었고, 줄 안의 공백 87자리가 개행이 되어 문장이 두 줄로
갈라졌으며, 강제 절단으로 원래 공백이 없던 18자리에서는 개행이 낱말 한가운데로 들어갔다 —
`Time Person of the Year (2021)`이 `Time Person of the Yea` / `r (2021)`로 실렸다.
겹침을 떼어 막으려던 '낱말 한가운데'가 절단면에서 되살아난 셈이다. 개행은 **겹침을 떼지 못한
이음매**에만 넣는다(옛 규칙으로 나뉜 청크, 메우지 못한 구멍) — 거기서는 두 조각이 원문에서
이어져 있다는 보장이 없어 붙여 쓰면 두 낱말이 한 낱말로 붙는다.
그래서 이 대조는 **공백 한 칸까지** 본다. 공백을 빼고 재면 위의 셋이 전부 통과한다.

**개수로 자르지 않는다.** 관련도 순으로 뽑아 놓고 '문서당 3건'처럼 개수로 깎으면 더 가까운 것을
버리고 더 먼 것을 싣게 된다 — 임베딩이 제 일을 했다면 한 문서가 상위를 차지하는 것은 정답이다.
고칠 것은 '몇 개를 버릴까'가 아니라 '흩어진 조각을 어떻게 다시 붙일까'였고, 조각 사이의 구멍은
관련도 문제가 아니라 청킹이 만든 인공물이므로 그것만 메운다.

남긴 것은 **글자 상한 하나**(`MAX_DOC_LEN`)뿐이고 이유도 하나다 — '이 문서가 답'이라는 판정이
틀렸을 때 다른 문서가 하나도 안 보이면 모델은 대안을 볼 수 없고, 그 오답은 오류를 남기지 않는다.
**원칙이 아니라 튜닝 값이다.** 근거는 `trace.search.top`의 거리 분포다(8-).

**검색은 이 상한을 겨냥하지 않는다.** 적중한 구간과 그 사이 구멍만 싣고, 그 밖으로 넓히는 것은
`expand`의 몫이다(2-3). 검색이 먼저 채우면 청구할 것이 남지 않아 모델이 왕복 하나를 헛되이 태운다.
다만 **적중 구간 자체가 넓으면 검색만으로도 상한에 닿는다** — 긴 문서에서는 후보 청크가 그 문서로
쏠려 계획된 범위가 문서 전체가 되고, 그때 실리는 항목은 대표 청크를 가운데 둔 상한 크기다(상한이 4,500자이던
때의 실측: 46청크 문서에서 `9~14/46`, 4,487자). 그 항목에는 `full`이 서므로 번호가 붙지 않고, 모델이 청구해
헛도는 일도 없다(2-3) — 상한은 '검색 + `expand` 합계'이므로 어느 쪽이 채웠든 결론은 같다.

### 2-6. 쿼리 목록이 채워지는 길

쿼리만 규칙이 다르다. 지식·처리방법은 '검색된 것이 전부'인데, 쿼리 목록은 **두 출처의 합집합**이고
항목마다 '자세히 실을지'가 따로 정해진다 (`agent.js selectQueries`).

| 출처 | 언제 | 자세한 줄로 오르나 |
|---|---|---|
| 경로A — 처리방법 본문이 이름을 언급한 쿼리 | `qa_method`를 찾았을 때 | 전부 |
| 직접 검색(`query`) 결과 | `query`를 검색했을 때 | 상위 `DETAIL_TOP`(5)건 |
| `resolveQuery` — 모델이 목록 밖 이름을 지목했을 때 | 그 스텝 이후 | 전부 |

경로A가 맨 앞이다. 처리방법이 "이 쿼리를 이 순서로"라고 말하는데 그 정의가 없으면 왕복만 하나 더 는다.

**등록 규모와 무관하게 두 출처의 결과만 싣는다.** 예전에는 등록이 `MAX_PROMPT_QUERIES`(30)건 이하면
직접 검색 결과와 무관하게 등록 전체를 이어 붙였다 — "설명이 얇아 검색에 안 걸린 쿼리도 이름은 보여야
모델이 지목할 수 있다"는 근거였다. 그런데 이 폴백은 `MAX_DIST`(`search.js`)가 이미 '관련 없음'으로
걸러낸 것까지 무효로 만들었다: 등록과 전혀 무관한 질문에도 소규모 설치에서는 등록 전체가 후보로
붙어(실측: "일론 머스크의 위기" 같은 질문에 업무 쿼리 5건이 그대로 실렸다) 모델이 억지로 하나를
골라야 할 것 같은 프롬프트가 됐다. 얇은 설명으로 인한 recall 손실은 경로A(절차가 이름으로 지목)가
이미 보완하므로, 지금은 등록 규모와 무관하게 경로A와 직접 검색 결과의 합집합만 `MAX_PROMPT_QUERIES`에서
잘라 싣는다 — 맞는 쿼리가 안 보이면 `query`를 다시 검색하라고 시스템 프롬프트가 말하는 것이 그래서다.

**자세한 표시는 병합을 넘어 옮겨진다.** 병합은 먼저 온 행을 지키므로(2-2) 두 번째 `query` 검색의 상위 적중이
이미 목록에 있으면 새 행으로 들어오지 못한다 — `detail` 표시만 옮겨 받는다(`agent.js absorb`). 그런 요청에서는
두 번째 검색이 새 항목을 하나도 넣지 못하므로, 옮긴 표시도 진도로 센다. 안 세면 검색어를 고쳐 다시 찾은
정당한 검색이 '새 자료 없음'으로 헛돈 스텝에 들어간다.

**자세한 줄과 짧은 줄.** 짧은 줄은 이름·용도·바인드(·후보가 둘 이상이면 대상DB)뿐이고, 자세한 줄에는
입출력 설명과 SQL 원문이 더 붙는다. **예산이 남아도 `detail` 표시가 없으면 올리지 않는다.** 고르는 근거는
이름·용도·바인드이고, 지목하면 다음 스텝에 자세히 실리며(`resolveQuery`), 실행하면 결과 컬럼이 이력에
보인다. 검색이 요청 시에만 도는 구조에서는 예산이 늘 남으므로, 올릴 수 있을 때 전부 올리면 등록 30건 ×
SQL 원문이 스텝마다 다시 실린다 — prefill이 스텝 수만큼 곱해지는 자리다.

그래서 이 섹션의 통상 사용량은 자기 몫(3-1)에 견줘 **1천 자 안팎**이다 — 몫의 대부분이 남는다.

---

## 3. 예산

### 3-1. 전체와 섹션

**전체 상한 하나 + 섹션별 기본 몫과 천장**이다. 섹션마다 독립 상한을 두면 합계가 문서와 어긋나도 드러나지 않고,
상한 없는 섹션 하나가 나머지 전부를 무의미하게 만든다.

| 항목 | 기본 | 천장 | 뜻 |
|---|---|---|---|
| `MAX_PROMPT_TOTAL_LEN` | 80,000자 | | 자료 네 섹션의 합계 천장 |
| `PROMPT_FRAME_RESERVE` | 400자 | | 제목 줄·빈 줄·지시 블록의 몫 |
| `PROMPT_FLOORS.history` | 25,000자 | 없음 | 다른 섹션이 아무리 길어도 보장되는 몫. 줄 수 × 스텝 상한으로 묶여 기본보다 커지지 않는다 |
| `PROMPT_FLOORS.queries` | 15,000자 | `PROMPT_CEILINGS.queries` 20,000자 | 짧은 줄 30건은 기본 안에(최악 10,506자), 자세한 줄 30건은 천장 안에 |
| `PROMPT_FLOORS.qaMethods` | 10,000자 | `PROMPT_CEILINGS.qaMethods` 20,000자 | 1,000자 항목이면 기본에 9건, 천장에 19건(실측) |
| `PROMPT_FLOORS.knowledge` | 25,000자 | `PROMPT_CEILINGS.knowledge` 40,000자 | 10,000자 매뉴얼 창이 기본 2건, 천장 4건 |
| 기본 합계 | 75,400자 | | **여유 4,600자** |

배분 순서(= `PROMPT_FLOORS` 선언 순서)가 곧 우선순위다. 각 섹션의 몫은 `min(천장, max(기본, 남은 예산 − 뒤 섹션들의
기본 합))`이고, 실제로 쓴 만큼만 빠져 다음 섹션으로 넘어간다(`llm-openai.js renderSections`). 뒤 섹션들의 기본을
미리 떼어놓으므로 앞이 뒤를 굶기지 못하고, 앞이 남긴 여유는 뒤로 흐르되 천장에서 멈춘다. **뒤가 남긴 것은 앞으로
돌아오지 않는다.**

- 실행 이력 — 잘리면 이미 조회해둔 결과를 버리고 같은 질문에 다시 매달린다. 맨 앞이다.
- 쿼리 목록 — 잘리면 그 조회를 아예 못 한다. 짧은 줄 보장(2-6)이 기본 몫 안에서 서고, 여유는 `detail` 표시가 붙은 것만 쓴다.
- 처리방법 — 절차와 쿼리 이름을 지목해 라우팅을 이끈다. 잘리면 조회 경로가 끊기므로 지식보다 앞에서 여유를 먼저 집는다.
- 지식 — 잘리면 답의 근거만 얇아진다. 매뉴얼처럼 길어(`MAX_DOC_LEN`) 가장 큰 천장을 받고, 맨 뒤라 나머지 여유를 전부 받는다.

**옛 순서(지식 → 처리방법 → 이력 → 쿼리)는 여유를 버리고 있었다.** 맨 뒤의 쿼리 목록이 여유를 전부 받았는데 그 섹션은
`detail` 표시가 없으면 여유를 쓰지 않으므로, 지식·처리방법이 후보를 꽉 채운 통상 요청을 실제로 조립해도 자료 네 섹션의
합은 64,000자 중 24,513자였고 지식 11건·처리방법 10건이 잘린 채였다(실측). 지식은 뒤 세 몫을 떼고 나면 기본 + 200자에
묶여 있었다 — 이름은 "최소 몫"인데 사실상 고정 상한이었다.

**천장은 응답 속도의 손잡이다.** 예산 불변식과는 무관하다 — 총량 안에서 남은 것만 받는다. 천장이 없으면 긴 문서 요청에서
지식 하나가 50k자를 먹고, 그 prefill이 스텝마다 곱해진다(prefix caching이 재사용하는 것은 앞부분뿐이고 자료 섹션은
검색이 돌 때마다 바뀐다). 통상 요청은 후보가 그만큼 있을 때만 커진다 — 1,000자 글 20건이면 20k에서 저절로 멈춘다.
`[llm] usage prompt=`과 `[agent] timing llm=`(8-)이 느려지면 총량이 아니라 천장부터 낮춘다. 후보를 더 싣고 싶은데
프롬프트를 늘리기 싫으면 문서당 상한(`MAX_DOC_LEN`)을 낮춰 같은 몫에 더 많은 건수를 넣거나, 애초에 덜 받도록
`SEARCH_LIMIT`을 조정한다.

이력에 천장이 없는 이유: 실사용이 줄 수(`MAX_HISTORY_ROWS`) × 스텝 상한(`MAX_PROMPT_STEP_LEN`)으로 묶여 기본 몫보다
커질 수 없다 — 천장을 적어도 아무 일도 하지 않는다. 기본을 20,000자로 낮추려면 스텝 상한을 2,000자로 내려야 한다
(필요 몫 18,552자) — 조회 결과를 덜 보여주는 대가라 25,000자를 골랐다(필요 23,557자).

예산을 넘기면 **꼬리부터 버리고 몇 건을 버렸는지 모델에게 알린다.** 실행 이력만 반대로 앞에서부터 버린다
(최신 기록이 가장 중요하다). 쿼리 목록은 버리기 전에 줄인다 — 먼저 모든 쿼리의 짧은 줄을 확보하고,
그중 `detail` 표시가 붙은 것만 예산이 허락하는 만큼 자세한 줄로 올린다(2-6). 짧은 줄로도 다 못 실을 만큼
예산이 모자라면 그때는 꼬리부터 버린다.

**목록에 오른** 쿼리가 프롬프트에서 통째로 사라지는 일은 없다. "등록한"이 아니다 — 등록이 30건을 넘으면
목록에 오르는 단계에서 이미 걸러지고, 그것은 예산이 아니라 라우팅이 하는 일이다(2-6).

### 3-2. 항목 하나의 상한

| 항목 | 값 | 어디에 |
|---|---|---|
| `MAX_PROMPT_ITEM_LEN` | 1,000자 | 지식·처리방법 본문 1건 = 청크 상한 (`chunk.js CHUNK_MAX_LEN`) |
| `MAX_DOC_LEN` | 10,000자 | 한 문서가 지식 섹션에서 차지할 수 있는 최대 (검색 + `expand` 합계, 프롬프트에 실리는 형태 기준 — 2-5). 한 번의 청구로 이 창을 채우도록 `agent.js GROW_WINDOW`(12청크)가 창 ÷ 청크 크기 이상이다 |
| `MAX_EXPANDED_ITEM_LEN` | 4,500자 | 청크가 아닌 항목(처리방법)을 펼쳤을 때의 본문 상한 (llm-openai.js itemLine). 문서 창과 따로 두어 창을 넓혀도 처리방법 몫이 따라 커지지 않는다 |
| `CHUNK_TARGET_LEN` | 900자 | 청크 목표 크기 (chunk.js) |
| `CHUNK_OVERLAP` | 150자 | 청크 겹침 (chunk.js) |
| `MAX_PROMPT_NAME_LEN` | 100자 | 제목·쿼리명 (llm-openai.js) |
| `MAX_PROMPT_SQL_LEN` | 800자 | 쿼리 SQL 원문 |
| `MAX_PROMPT_SHORT_DESC_LEN` | 120자 | 짧은 형태의 쿼리 용도 |
| `MAX_PROMPT_STEP_LEN` | 3,000자 | 실행 이력 1스텝의 결과 JSON |
| `MAX_PROMPT_PARAMS_LEN` | 500자 | 실행 이력 1줄의 params 표시 |
| `MAX_PROMPT_SEARCH_LEN` | 200자 | 실행 이력의 검색어 표시 |

### 3-3. 예산 밖의 몫

| 항목 | 값 | 왜 밖인가 |
|---|---|---|
| 최근 대화 | 6턴 × 1,500자 = 9,000자 (`MAX_CHAT_TURNS` · `MAX_CHAT_LEN`) | 이미 다른 상한으로 묶여 있다 |
| 질문 | 2,000자 (`MAX_QUESTION_LEN`) | 서버 입력 검증이 묶는다 |
| 시스템 프롬프트 | 6,000자 (`MAX_SYSTEM_PROMPT_LEN`, 실측 ~5,000자) | 요청마다 같아 prefix caching이 재사용한다 |

최악 합계는 80,000 + 9,000 + 2,000 + 6,000 = **97,000자** ≈ 54k 토큰, 한국어에 덜 우호적인 토크나이저라도
≈ 65k다. 128k 컨텍스트의 42~50%이고 출력 가드(`MAX_COMPLETION_TOKENS` 16,384)를 더해도 55~63%다. 최악은 네 섹션이
천장이 아니라 총량까지 찬 요청이라 통상 요청과는 멀다(3-1).

세 몫 **모두** 상수가 묶는다. 시스템 프롬프트만 오래도록 관측값이어서 규칙 한 줄을 더할 때마다 조용히
자랐고, 이 표가 실제의 절반도 안 되는 값을 가리키고 있었다(~2k자라 적고 실측 4.9k자). 예산 밖이라는 것은
"세지 않아도 된다"가 아니라 "다른 상한이 센다"는 뜻이므로, 그 상한을 만들어 `llm-openai.js`가 로드 시
검증한다.

---

## 4. 횟수와 줄 수

| 항목 | 값 | 뜻 |
|---|---|---|
| `MAX_SEARCHES` | 3 | 한 요청의 검색 횟수 (환경변수로 낮출 수만 있다) |
| `SEARCH_LIMIT` | 20 | 검색 한 번이 소스당 돌려주는 후보 수 (〃). 프롬프트에 실리는 것은 몫 ÷ **항목의 실제 크기**라 등록된 글의 길이가 정한다 — 2~20건 (2-2) |
| `MAX_STEPS` | 5 | 한 요청의 조회 수 |
| `MAX_BATCH_QUERIES` | 4 | 결정 하나에 담을 수 있는 조회 수 |
| `MAX_EXPANDS` | 2 | 한 요청에서 본문을 넓힌 횟수 — 항목 하나를 한 번 넓힌 것이 1 (항목 수도 결정 수도 아니다 — 2-3) |
| `MAX_DROPS` | 40 | 결정 하나가 버릴 수 있는 항목 수 |
| `MAX_HISTORY_ROWS` | 9 | 실행 이력의 줄 수 = `MAX_STEPS` + `MAX_SEARCHES` + 안내 1 |
| `MAX_PROMPT_QUERIES` | 30 | 프롬프트에 싣는 쿼리 수 (agent.js). 등록이 이 수를 넘으면 라우팅으로 갈린다 (2-6) |
| `DETAIL_TOP` | 5 | 직접 검색 중 자세한 형태로 싣는 상위 건수 (agent.js) |
| `MAX_GUARD_HITS` | 2 | 연속으로 헛돌면 강제 답변으로 간다 (agent.js). 헛돈 스텝 = 새 자료 없는 검색, 늘지 않은 청구, 실행 없이 끝난 조회(중복·미등록 이름, 그리고 `oracle.js wastedStep` — 대상 DB 미선택·등록 SQL 실행 불가·접속 정보 없음 같은 등록·설정 오류) |
| `MAX_LOOP_MS` | 180,000 | 요청 시작부터 재는 시간 예산 (agent.js) |
| 루프 반복 | `MAX_STEPS`+`MAX_SEARCHES`+`MAX_EXPANDS` = 10 | 한 요청의 결정(LLM 왕복) 수 (agent.js) |

**줄 수를 직접 세는 이유.** 일괄 조회 전에는 루프 반복 수가 곧 이력 줄 수여서 저절로 지켜졌는데,
결정 하나가 조회 여럿을 만들게 되면서 그 등식이 깨졌다. 넘겨 기록하면 프롬프트의 이력 몫이 보장하는
"전부 실린다"가 조용히 무너지고, 가장 오래된 조회 결과가 빠진다.

**안내 줄 한 자리(+1)는 나눠 쓴다.** 상한에 걸려 잘린 일괄 조회 항목의 안내와 본문 청구 실패 안내가 그
한 자리를 공유한다. 자리가 없으면 안내를 접는데, 그때는 루프가 곧 줄 수 상한에서 멈추므로 모델이 그 안내를
읽을 스텝 자체가 없다. 잘린 배치의 안내는 **어느 상한에 걸렸는지**를 말한다 — 조회 수(`MAX_STEPS`)가 아니라
줄 수(`MAX_HISTORY_ROWS`)가 막은 배치에 '조회 스텝 상한'이라고 적으면 모델은 사실과 다른 이유를 받는다.

**`MAX_STEPS + MAX_SEARCHES + 1`은 합계일 뿐 모양별 회계가 아니다.** 개수가 실제로 묶여 있는 것은 쿼리
결과·오류 줄뿐이다(`runs`가 가드에 걸린 항목까지 함께 센다). 검색 줄은 그렇지 않다 — 중복 검색·횟수 상한에
걸린 줄은 이력에 검색 모양으로 남지만 `searches`를 올리지 않으므로, 진도가 난 검색과 번갈아 나오면 검색
모양 줄이 `MAX_SEARCHES`를 넘는다(가드는 '연속'으로 헛돌 때만 끊는다). 그래서 이력 몫의 검증은 조합을
세지 않고 **쿼리 줄 `MAX_STEPS`개 + 나머지 자리는 전부 가장 긴 비쿼리 줄**로 잡는다. 조합을 세던 계산이
최악을 맞힌 것은 검색 줄이 쿼리 줄보다 짧다는 사실 덕분이었을 뿐이고, 실제로 157자 모자랐다.

---

## 5. 불변식 — 깨지면 소리가 나야 한다

| 불변식 | 지키는 곳 | 깨지면 |
|---|---|---|
| 섹션 최소 몫의 합 + 고정 틀 ≤ 전체 상한 | `constants.js` 로드 시 `throw` | 서버가 뜨지 않는다 |
| 이력 몫이 `MAX_HISTORY_ROWS` 줄을 **어떤 조합으로든** 전부 담는다 | `llm-openai.js` 로드 시 `throw` | 가장 오래된 조회 결과가 빠진다 |
| 펼침 총량 — 지식은 `MAX_EXPANDS` × `MAX_DOC_LEN` = 20,000자 < 지식 기본 몫, 처리방법은 `MAX_EXPANDS` × `MAX_EXPANDED_ITEM_LEN` = 9,000자 < 처리방법 기본 몫 | `test/constants.test.js` | 넓힌 구간이 다시 잘린다 |
| 천장은 기본 몫이 있는 섹션에만, 기본 이상으로 붙고, 앞 섹션이 비면 닿을 수 있다 | `constants.js` 로드 시 `throw` · `test/constants.test.js` | 기본 몫 보장이 조용히 눌리거나, 천장이 한 번도 닿지 못하는 숫자가 된다 |
| 청크 상한 = `MAX_PROMPT_ITEM_LEN` | `test/chunk.test.js` | 청크마다 잘림 표시가 붙고 청구 번호가 잘림 기준으로 되돌아간다 |
| 청크 항목은 잘리지 않아도 번호가 붙는다 | `test/prompt.test.js` | 긴 문서에 번호가 한 번도 안 붙어 청구 경로가 통째로 죽는다 |
| 번호가 붙은 청크 항목은 청구하면 반드시 늘어난다 (`full`) | `test/chunk.test.js` · `test/agent.test.js` | 청구가 헛돌고, 두 번이면 강제 답변으로 넘어간다 |
| 청크 항목은 줄이 많아도 프롬프트에서 잘리지 않는다 | `test/chunk.test.js` | 실린 범위의 꼬리가 사라지고 잘림 표시가 붙는다 |
| 이어 붙인 구간은 원문 그대로다 — **공백 한 칸까지** | `test/chunk.test.js` | 이음매마다 150자가 되풀이되거나, 문단 경계가 사라지고 낱말이 개행으로 갈라진다 |
| 찾아보지 않은 대상은 이력 줄에 `0건`으로 적히지 않는다 | `test/agent.test.js` | 모델이 `query` 검색을 이미 한 것으로 읽고 건너뛴다 |
| 같은 문서가 두 항목으로 들어오지 않는다 | `test/agent.test.js` | 모델이 같은 글을 두 번 읽고 지식 몫을 두 번 먹는다 |
| 뒤 검색이 찾은 같은 문서의 다른 구간은 같은 번호로 실린다 | `test/agent.test.js` | 그 절이 존재한다는 사실조차 모델이 볼 수 없다 |
| 펼친 항목이 이후 검색에 밀리지 않는다 | `test/agent.test.js` | 청구한 구간이 사라지고 청구 기회만 잃는다 |
| 어떤 섹션이 길어져도 **자료 네 섹션 + 고정 틀** ≤ 전체 상한 | `test/prompt.test.js` | 그 뒤 모든 질문이 컨텍스트 초과로 실패 |
| 시스템 프롬프트 ≤ `MAX_SYSTEM_PROMPT_LEN` | `llm-openai.js` 로드 시 `throw` | 예산 밖의 몫이 조용히 자라 3-3의 합계가 근거를 잃는다 |
| 환경변수를 실제로 채워도 모듈이 뜬다 | `test/constants.test.js` (새 프로세스) | 문서대로 설정한 설치만 죽는다 |
| 이 문서의 숫자·순서가 코드와 같다 | `test/context-doc.test.js` | 다음 사람이 틀린 숫자를 근거로 판단한다 |

로드 시점에 터뜨리는 이유: 예산이 어긋난 채로 뜨면 등록이 늘어난 뒤에야, 그것도 원인이 안 보이는 형태로
드러난다.

---

## 6. 값을 고칠 때 함께 볼 것

| 고치는 값 | 함께 봐야 할 것 |
|---|---|
| `MAX_PROMPT_TOTAL_LEN` | `PROMPT_FLOORS` 합계, `MAX_ANSWER_LEN`, `MAX_COMPLETION_TOKENS` (둘의 합이 컨텍스트 안에) |
| `PROMPT_FLOORS.*` · `PROMPT_CEILINGS.*` | 합계 검증, 이력 몫 검증, 천장 ≥ 기본과 닿을 수 있는지, `test/prompt.test.js`의 최악 조합과 여유 방향. 선언 순서가 배분 순서다(3-1) |
| `MAX_STEPS` · `MAX_SEARCHES` | `MAX_HISTORY_ROWS`, 이력 몫 검증 |
| `MAX_EXPANDS` · `MAX_DOC_LEN` · `MAX_EXPANDED_ITEM_LEN` | 지식 기본 몫(`MAX_EXPANDS` × `MAX_DOC_LEN`)과 처리방법 기본 몫(`MAX_EXPANDS` × `MAX_EXPANDED_ITEM_LEN`)에 각각 들어야 한다, `agent.js GROW_WINDOW`(창 ÷ `CHUNK_TARGET_LEN` 이상), `agent.js mergeFront`의 핀 고정, 재는 자는 `constants.js indentLines`(2-5) |
| `MAX_PROMPT_ITEM_LEN` | `chunk.js CHUNK_MAX_LEN` (같아야 한다 — 2-5). 지식 항목의 크기는 이 값이 아니라 `MAX_DOC_LEN`이 정한다(2-2) — 실리는 건수를 바꾸려면 그쪽을 본다 |
| `CHUNK_TARGET_LEN` · `CHUNK_OVERLAP` · `CHUNK_MAX_LEN` · `CHUNK_SPLIT_VERSION` | 문서 해시에 들어간다 — 고치면 다음 동기화가 전 문서를 다시 나누고, 내용이 달라진 청크를 다시 임베딩한다 (대량 설치에서는 한산한 시간에). 절단 규칙을 고쳤으면 판을 올린다 |
| `SEARCH_COLUMNS` | `vec_<소스>` 테이블이 있어야 한다 (`schema.sql`), 기존 설치는 `sql/migrate-chunk.sql` |
| `MAX_SYSTEM_PROMPT_LEN` | 3-3의 최악 합계, `MAX_COMPLETION_TOKENS` (둘의 합이 컨텍스트 안에) |
| 이력 줄의 안내 문구 · `MAX_PROMPT_SEARCH_LEN` | 이력 몫 검증 (비쿼리 줄이 쿼리 줄보다 길어지면 필요한 몫이 뛴다) |
| `MAX_BATCH_QUERIES` | `oracle.js POOL_MAX`(이 값 × 2), `db.js CONNS_PER_REQUEST` |
| `SEARCH_LIMIT` | 지식·처리방법 몫 (후보 수 × 항목 크기 — 지식은 `MAX_DOC_LEN`까지, 2-2) |
| `MAX_PROMPT_QUERIES` · `DETAIL_TOP` | 라우팅 판정(2-6), 쿼리 몫, `db.js`가 읽는 상한+1건 |
| `MAX_TABLE_INJECT_LEN` · `MAX_CHART_INJECT_LEN` | `MAX_ANSWER_LEN` (셋의 합이 응답 본문과 `chat_log.answer`의 천장) |
| `MAX_CELL_LEN` · `MAX_RESULT_COLS` | `MAX_PROMPT_STEP_LEN`, 이력 몫 검증 |
| `MAX_CHAT_TURNS` · `MAX_CHAT_LEN` | 프런트 `App.jsx`의 같은 값 (페이로드를 그 기준으로 자른다) |

---

## 7. 컨텍스트 밖으로 나가는 것

프롬프트에 실리는 것과 **사용자에게 보이는 것은 다르다.**

| 값 | 프롬프트 | 화면 | chat_log |
|---|---|---|---|
| 조회 결과 행 | 20행 (`MAX_RESULT_ROWS`) | 조회된 전부 (`MAX_ROWS` 1,000행까지) | 20행 |
| 드라이버·DB 오류 원문 | 실린다 | 일반 문구로 바꾼다 | 실린다 |
| 모델 전용 복구 지침(`hint`) | 실린다 | 나가지 않는다 | 실린다 |
| 루프 가드 기록(`note`) | 실린다 | 나가지 않는다 | 실린다 |
| 자료 번호(`k12`·`m3`) | 실린다 | 나가면 안 된다 (프롬프트 규칙) | — |
| 지식 구간 표기(`(9~14/46)`) | 실린다 | 폴백 답변에 실린다 (2-5) | 답변 그대로 |
| 잘린 셀 표시(`…(생략)`) | 실린다 (셀 200자, `oracle.js`) | 답변의 표·차트 칸에도 붙는다 — 그 칸은 더 짧게 잘리므로(`chart.js`) 표시 없이 자르면 사용자는 온전한 값으로 읽고, 다음 턴에 그 값을 옮겨 적은 바인드를 잘린 값 가드(`agent.js clippedCopyDetector`)가 알아보지 못한다(실측) | 답변 그대로 |

답변 안의 `` ```table `` · `` ```chart `` 참조는 **답이 확정된 뒤** 서버가 조회 결과로 채운다. 그래서 사용자에게
나가는 답변은 `MAX_ANSWER_LEN`(70,000자)보다 클 수 있다. 최악은 70,000 + `MAX_TABLE_INJECT_LEN`(30,000자)
+ `MAX_CHART_INJECT_LEN`(30,000자) = **130,000자**이고(둘 다 `chart.js`), 그 값이 응답 본문과
`chat_log.answer`의 실제 천장이다.

---

## 8. 계측

값을 고칠 근거는 추측이 아니라 `chat_log`와 서버 로그다.

- `[agent] timing total=… llm=N(…ms, prompt … tok) search=… oracle=…` — 요청당 구간별 소요.
  `timing.oracle`의 항목은 조회 한 번이 아니라 **결정 한 번의 경과**다 (일괄 조회는 병렬로 돈다).
- `trace.search` — `searches`, 대상별 횟수, 대상별 적중 수, `expanded`, `dropped`,
  `searchFailed`, `queriesFailed`. 적중 수는 '검색이 성립한 적 있는' 대상만 숫자다 — 한 번도 찾지
  않았거나 찾을 때마다 실패한 대상은 `null`이다(0으로 적으면 '찾았는데 없다'와 섞인다). `queries`도
  같은 규칙이다 — `query`를 한 번이라도 성립하게 검색했으면 숫자, 경로A(처리방법이 지목한 쿼리)만
  돌았으면 `null`이다(2-6).
- `trace.search.top` — 지식 적중 상위 5건의 `{doc, d}`(문서 seq와 코사인 거리). **문서당 상한
  (`MAX_DOC_LEN`)이 옳은지는 논증이 아니라 이 숫자로 갈린다.** 한 문서가 상위를 차지할 때 거리가
  촘촘하게 낮으면(0.30~0.40) 그 문서가 정답이니 상한을 올릴 근거이고, 문턱 근처에 흩어져 있으면
  (0.48~0.55) 긴 문서가 청크 수로 밀고 들어온 것이니 유지할 근거다.
- `[embed] … rechunked N (replacing M)` — 청크 재생성. 평상시 주기에는 나오지 않는다 (문서가 바뀐 주기에만).
- `[llm] usage prompt=… completion=…` — 서버가 알려준 토큰 실측. 문자 기준 예산을 다시 잡을 때 쓴다.

조정 질의는 README의 "검색 후보 수·검색 횟수 조정" 절에 있다.
